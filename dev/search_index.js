var documenterSearchIndex = {"docs":
[{"category":"section","location":"double_pendulum/#Double-Pendulum","page":"Double Pendulum","text":"The double pendulum is a chaotic, strongly nonlinear Hamiltonian system. It is built here with hodeproblem (the canonical Hamiltonian form), so the implicit midpoint equations require genuine nonlinear iterations at every step. Because the Hamiltonian H(t q p) depends on both the coordinates q and the momenta p, the energy-drift proxy is evaluated from the full (q p) state.\n\nBecause the forces are O(g m l), this problem's nonlinear residual bottoms out well above varepsilon(T), so the solver tolerance is relaxed to 256varepsilon(T). That floor is a property of the problem's scale, not of the precision: measured, it sits at approx 200 dots 260varepsilon(T) at every precision. It is also why the results table below carries an at_tolerance column with every row flagged — on this problem a converged run stops at its target rather than below it. Read the caveat literally at 16-bit: 256varepsilon(textttBFloat16) is 20, comparable to the energy itself, so a BFloat16 row here reports having met a target too loose to say much.\n\nThe benchmark below is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/midpoint_double_pendulum.jl for accurate BenchmarkTools measurements. The results are shown first for the standard step Delta t = 001 and then repeated for a coarse step Delta t = 01 (see Coarse time step (Δt = 0.1)).\n\nusing SolverBenchmark\n\nspec = double_pendulum_spec(timespan = (0.0, 10.0), timestep = 0.01)\ndf   = cached_sweep(\"double_pendulum_dt0.01\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Double Pendulum"},{"category":"section","location":"double_pendulum/#Convergence","page":"Double Pendulum","text":"plot_convergence(df; title = \"Double Pendulum\")","title":"Convergence"},{"category":"section","location":"double_pendulum/#Nonlinear-iterations","page":"Double Pendulum","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df)","title":"Nonlinear iterations"},{"category":"section","location":"double_pendulum/#Run-time","page":"Double Pendulum","text":"plot_runtime(df)","title":"Run time"},{"category":"section","location":"double_pendulum/#Energy-drift","page":"Double Pendulum","text":"Drift of the conserved energy H(t q p) of the double pendulum:\n\nplot_energy_drift(df)","title":"Energy drift"},{"category":"section","location":"double_pendulum/#Discussion","page":"Double Pendulum","text":"The problem is chaotic and nonlinear, so Newton needs genuine iterations at every step. All six line searches behave similarly; Newton/Quadratic is the weakest of them but still converges for two thirds of the runs. Picard never converges here, and at Δt = 0.01 it is the only failure at Float32/Float64; at the coarse step DogLeg joins it.\nPrecision dominates: 21/24 at both Float32 and Float64, 11/24 at Float16 and 5/24 at BFloat16 (58/96 overall; 64/96 at the coarse step). Every converged row is flagged in the at_tolerance column, because on this problem the residual floor is the tolerance — see the note above.\nNo closed-form solution is available, so accuracy is judged solely through the energy drift, which scales with the floating point precision.","title":"Discussion"},{"category":"section","location":"double_pendulum/#Results-table","page":"Double Pendulum","text":"markdown_table(summary_table(df))","title":"Results table"},{"category":"section","location":"double_pendulum/#double_pendulum_dt01","page":"Double Pendulum","text":"With a ten times larger step the equations become more strongly nonlinear, which stresses the solvers noticeably more than the Delta t = 001 results above.\n\nspec1 = double_pendulum_spec(timespan = (0.0, 10.0), timestep = 0.1)\ndf1   = cached_sweep(\"double_pendulum_dt0.1\") do\n    run_benchmark(spec1; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Coarse time step (Δt = 0.1)"},{"category":"section","location":"double_pendulum/#Convergence-2","page":"Double Pendulum","text":"plot_convergence(df1; title = \"Double Pendulum (Δt = 0.1)\")","title":"Convergence"},{"category":"section","location":"double_pendulum/#Nonlinear-iterations-2","page":"Double Pendulum","text":"plot_iterations(df1)","title":"Nonlinear iterations"},{"category":"section","location":"double_pendulum/#Run-time-2","page":"Double Pendulum","text":"plot_runtime(df1)","title":"Run time"},{"category":"section","location":"double_pendulum/#Energy-drift-2","page":"Double Pendulum","text":"plot_energy_drift(df1)","title":"Energy drift"},{"category":"section","location":"double_pendulum/#Results-table-2","page":"Double Pendulum","text":"markdown_table(summary_table(df1))","title":"Results table"},{"category":"section","location":"precision/#Low-Precision-Support","page":"Low-Precision Support","text":"Sweeping four precisions is the point of this package, and the two 16-bit formats are where most of the friction is. This page collects what had to be done to make them run at all, what they can and cannot represent, and how the residual tolerance scales. For what the sweeps actually measured, see Key Findings.\n\nFloat16 and BFloat16 divide the same 16 bits differently:\n\n Significand bits Exponent bits eps(T) floatmax(T)\nFloat16 11 5 9.8e-4 6.5e4\nBFloat16 8 8 7.8e-3 3.4e38\n\nBFloat16 therefore has the dynamic range of Float32 and roughly a third of Float16's resolution. Sweeping both separates a failure caused by too few digits from one caused by too little range — which is exactly why both are in default_precisions.","title":"Low-Precision Support"},{"category":"section","location":"precision/#The-BFloat16-compatibility-layer","page":"Low-Precision Support","text":"BFloat16 is not supported by this stack out of the box. src/bfloat16.jl fills the gaps. Every method in it is type piracy over an upstream omission, every one is the same widen-to-Float32-and-round (exact, because Float32(::BFloat16) is exact and Float32 has more than twice the significand), and every one should be deleted as BFloat16s.jl and NaNMath.jl grow the methods themselves.","title":"The BFloat16 compatibility layer"},{"category":"section","location":"precision/#Reached-by-the-range-constructor","page":"Low-Precision Support","text":"Every GeometricSolution builds its TimeSeries as the range tbegin:Δt:tend. Because BFloat16 is not a member of Union{Float16,Float32,Float64}, that range does not take Julia's twice-precision fast path but the generic ArithmeticRounds one, StepRangeLen(start, step, convert(Integer, fld(stop - start, step)) + 1) (base/range.jl). That needs:\n\nBase.rem — BFloat16s defines + - * / ^ for two BFloat16s but not rem, so it reaches Base's no_op_err stub. mod, div, fld and cld are all derived from rem and follow for free.\nBase.Integer — defined in Core for Union{Float16,Float32,Float64} only.\n\nWithout these, every run fails.","title":"Reached by the range constructor"},{"category":"section","location":"precision/#Reached-by-the-problem-right-hand-sides","page":"Low-Precision Support","text":"Base.sincos — this one recurses until the stack overflows, rather than raising a MethodError. base/special/trig.jl has sincos(x) = _sincos(float(x)) and _sincos(x::AbstractFloat) = sincos(x), so any AbstractFloat without a concrete sincos loops forever. The pendulum LODE's Lagrangian hits it.\nBase.atan(y, x), Base.fma, Base.mod2pi. One-argument atan, muladd, hypot, sincospi and sqrt are fine as they stand.\nNaNMath.{sin,cos,tan,asin,acos,atanh,acosh,log,log2,log10,log1p}. GeometricProblems writes its right-hand sides against NaNMath, whose NaN-returning variants are declared for Union{Float16,Float32,Float64}; everything else falls to NaNMath.f(x::Real), which deliberately throws a MethodError when float(x) === x. Hence the direct NaNMath dependency. Without this group four of the six implicit-midpoint problems appear not to converge at BFloat16.\n\nNothing else needed changing. ForwardDiff duals, the generic LU factorization and all six SimpleSolvers line searches specialise on BFloat16 as they stand.\n\nwarning: These gaps are invisible in the results\nrun_case records any exception as a non-converged row, so each of the above first presented as \"BFloat16 does not converge on this problem\" — with no error anywhere in the output. See Errors are recorded, not raised.","title":"Reached by the problem right-hand sides"},{"category":"section","location":"precision/#BFloat16-cannot-resolve-a-fine-time-grid","page":"Low-Precision Support","text":"This is a real limitation rather than a missing method, and it is easy to mistake for a solver failure.\n\nWith 8 significand bits the spacing of BFloat16 at t = 100 is 0.5 — five times a Delta t = 01 step. The time series therefore collides: 0:0.1:100 holds 1001 points but only 451 distinct values. HermiteExtrapolation and MidpointExtrapolation are then handed two identical times and throw ArgumentError: t₀ and t₁ in Hermite extrapolation are identical! — for every solver. NoInitialGuess, which never differences times, is unaffected.\n\nA BFloat16 sweep that fails exactly two of its three initial guesses is this, not a solver problem. Float16, with 11 significand bits, has spacing 0.0625 at t = 100 and resolves the same grid.\n\nNote the direction of the effect: a larger time step makes it better, against every other trend in the study. The harmonic oscillator converges 8/24 at Delta t = 01 and 24/24 at Delta t = 10.\n\ntip: Only compare the 16-bit formats where the grid resolves\nAny sweep whose Delta t is below the BFloat16 spacing at tend loses two of its three initial guesses for reasons unrelated to the solver. The Delta t = 001 sweeps over (0 10) are confounded this way (spacing 0.0625); the Delta t = 01 and Delta t = 10 sweeps are the honest comparisons.","title":"BFloat16 cannot resolve a fine time grid"},{"category":"section","location":"precision/#How-the-residual-tolerance-scales","page":"Low-Precision Support","text":"The solver's absolute residual tolerance is f_abstol_factor * eps(T), with the factor stored per problem on ProblemSpec and defaulting to 8 — the framework's own default (see Solver options).\n\nThe factor is a property of the problem, not of the precision. On the double pendulum the residual floor measures approx 200 dots 260varepsilon(T) at every precision:\n\nPrecision max_residual at Delta t = 001 div varepsilon(T)\nBFloat16 1.2 – 1.5 154 – 192\nFloat16 0.19 – 0.25 195 – 256\nFloat32 2.8e-5 – 3.1e-5 235 – 260\nFloat64 5.0e-14 – 5.7e-14 225 – 257\n\nThe floor is set by the problem's O(g m l) force scale, which varepsilon(T) already tracks — so a precision-dependent factor has nothing left to compensate for. Measured against the data, a 8-at-16-bit / 256-above rule is a strict loss: inert on all four LODEs, dominated by a uniform 8 on the Toda lattice, and on the double pendulum it drops both 16-bit formats to approx 0/24 by turning \"converged at the floor\" into \"failed\".\n\nTwo specs are worth knowing about:\n\nThe double pendulum keeps f_abstol_factor = 256, and needs it: at the default 8 its 16-bit columns are empty (0/24 each against 5/24 and 11/24).\nThe Toda lattice does not, and uses the framework default: 256 there buys 3 converged runs of 96 at Delta t = 01 and 1 at Delta t = 10, while costing one to two orders of magnitude of residual on every run that converges either way.\n\nscripts/f_abstol_study.jl regenerates that comparison.\n\nnote: At 16-bit, a relaxed tolerance can be larger than the quantity of interest\n256 eps(BFloat16) is 2.0 — comparable to the double pendulum's energy. A converged BFloat16 row there has met a target too loose to say much. This is why every results table records the f_abstol each run used and flags rows that stopped at it in an at_tolerance column; see summary_table.","title":"How the residual tolerance scales"},{"category":"section","location":"precision/#How-the-regularization-factor-scales","page":"Low-Precision Support","text":"The nonlinear-integrator sweep varies the solver's regularization_factor — the shift SimpleSolvers adds to the Newton Jacobian diagonal before factorizing it. Unlike f_abstol_factor, this one is a property of the precision, so it is swept as multiples of sqrtvarepsilon(T): that is the scale at which a Jacobian entry stops being distinguishable from its own rounding error, and an absolute shift chosen for Float64 falls below it at Float16, where it cannot lift a singular Jacobian at all. Scaling lambda is what keeps \"lambda too small\" separable from \"precision too low\".\n\nThe six rungs are spread over two ladders, because a range of multipliers that reaches a useful shift in double precision over-damps half precision several times over:\n\nPrecision sqrtvarepsilon(T) rung 1 rung 2 rung 3 rung 4 rung 5 rung 6\nBFloat16 8.84e-2 0.177 0.354 0.707 1.414 2.828 5.656\nFloat16 3.13e-2 0.0625 0.125 0.25 0.5 1.0 2.0\nFloat32 3.45e-4 6.91e-4 1.38e-3 2.76e-3 5.52e-3 1.10e-2 2.21e-2\nFloat64 1.49e-8 5.96e-8 2.38e-7 9.54e-7 3.81e-6 1.53e-5 6.10e-5\n\nBFloat16, Float16 and Float32 use 2^k sqrtvarepsilon(T) for k = 1 dots 6; Float64 uses k = 2 4 dots 12. The bold entries are 16sqrtvarepsilon(T), NonlinearIntegrators' recommended default — rung 4 on the reduced-precision ladder, rung 2 on the Float64 one — so each ladder is anchored on a shift already known to work and probes octaves either side of it. See scaled_regularization and regularization_exponent; what the ladder measured is in Key Findings.\n\nThe panels are labelled by rung, not by value. One benchmark DataFrame holds all four precisions and every plot and table groups on that label; since the two ladders share only two exponents, labelling by value would scatter the figures across eleven mostly-empty panels. Each row's own exponent and value are recorded in the regularization_exponent and regularization_factor columns of the CSV.\n\nwarning: Form the factor in `Float64`, then convert\nT(2.0^k * sqrt(Float64(eps(T)))) rather than T(2)^k * sqrt(eps(T)): the latter overflows to Inf for Float16 from k = 16 on, and since run_nonlinear_case records every exception as a non-converged row, that turns a rung into a silent non-run rather than an error.","title":"How the regularization factor scales"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Harmonic-Oscillator-(Nonlinear-Integrator)","page":"Harmonic Oscillator","text":"This page benchmarks the neural-network variational integrator NonLinear_OneLayer_GML from NonlinearIntegrators.jl on the harmonic oscillator, built as a Lagrangian problem (lodeproblem). The integrator represents the trajectory over one time step with a one-layer network (S = 4 neurons, activation x mapsto max(0x)^3) and enforces the discrete variational principle with an R = 8-point Gauss–Legendre quadrature; the network parameters are seeded with its built-in greedy (OGA1d) initial guess.\n\nUnlike the implicit midpoint analyses, this sweep varies the nonlinear solver's regularization factor lambda (a Levenberg–Marquardt-style shift added to the Newton Jacobian diagonal) in place of the initial guess, because the network solve is near-singular — without regularization Newton does not converge. The swept options are:\n\nDimension Values\nPrecision BFloat16, Float16, Float32, Float64\nSolver Newton/Static, Newton/Backtracking, Newton/StrongWolfe, DogLeg\nRegularization lambda 0, and rungs 1–6 of the sqrtvarepsilon(T) ladder\n\nEach run integrates ten time steps, and the three step sizes Delta t = 01 10 100 therefore span (01), (010) and (0100). The figures below panel by lambda; within each panel the solver configurations are on the x-axis and the precisions are distinguished by colour.\n\nnote: A rung is a different number at each precision\nlambda is swept as multiples of sqrtvarepsilon(T), not as absolute values, so that the shift is scaled to the precision it protects. Rung 4 is 16sqrtvarepsilon(T) — 0.5 at Float16, 5.5e-3 at Float32 — and the Float64 ladder is stretched to 2^k with k = 2 4 dots 12, where rung 2 is the same 16sqrtvarepsilon. How the regularization factor scales has the full table; each row's own value is in the results CSV.\n\nThe benchmark is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/nonlinear_harmonic_oscillator.jl for accurate BenchmarkTools measurements.\n\nusing SolverBenchmark\n\nspec = harmonic_oscillator_lode_spec(timespan = (0.0, 1.0), timestep = 0.1)\ndf   = cached_sweep(\"nonlinear_harmonic_oscillator_dt0.1\") do\n    run_nonlinear_benchmark(spec; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Harmonic Oscillator (Nonlinear Integrator)"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Convergence","page":"Harmonic Oscillator","text":"Which combinations reached the solver tolerance at every time step (green), and which failed (red):\n\nplot_convergence(df; panelcol = :regularization, title = \"Harmonic Oscillator (LODE)\")","title":"Convergence"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Nonlinear-iterations","page":"Harmonic Oscillator","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df; panelcol = :regularization)","title":"Nonlinear iterations"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Run-time","page":"Harmonic Oscillator","text":"plot_runtime(df; panelcol = :regularization)","title":"Run time"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Energy-drift","page":"Harmonic Oscillator","text":"Drift of the conserved energy H(t_textend) - H(0):\n\nplot_energy_drift(df; panelcol = :regularization)","title":"Energy drift"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Accuracy","page":"Harmonic Oscillator","text":"Maximum error against the analytic solution:\n\nplot_accuracy(df; panelcol = :regularization)","title":"Accuracy"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Discussion","page":"Harmonic Oscillator","text":"Regularization is essential, and it is a threshold rather than a tuned value. At lambda = 0 the Float64 Newton iteration converges for no solver: it runs out its 1000-iteration budget at a residual of approx 3 times 10^-12, above the tolerance, because the network parameterization makes the Jacobian near-singular. Every one of the six rungs fixes it, and they are indistinguishable from each other — 2.0–2.2 iterations per step, residual approx 5 times 10^-14, accuracy 141 to 144 times 10^-13 across a ladder spanning 6 times 10^-8 to 6 times 10^-5, a factor of a thousand. Nothing in this range over-damps; lambda only has to be nonzero.\nThe choice of line search barely matters once regularization is on: all of Static, Backtracking, StrongWolfe and DogLeg behave almost identically, because the regularized Newton step is already close to optimal.\nBoth 16-bit formats fail, and regularization cannot reach the reason. No configuration converges at Float16 or BFloat16 (0/28 each), at any rung. The failure is a SingularException — but not from the Newton Jacobian. It is raised in NonlinearIntegrators.initial_params!, by the G_k x_k = b Gram solve of the OGA initial guess, whose third selected neuron is already linearly dependent on its predecessors at 16 bits. That runs before the Newton iteration of every step, and regularization_factor shifts only the Newton Jacobian diagonal, so no rung of the ladder can lift it. Lifting it would mean regularizing the seed's normal equations upstream. BFloat16's wider exponent does not help — the obstacle is conditioning, which wants significand bits, and it has three fewer than Float16.\nFloat32 reaches its residual floor (approx 3 times 10^-5) in about one iteration per step and is reported as converged under the relaxed tolerance used for this problem (256varepsilon); its accuracy against the analytic solution is approx 5 times 10^-7. Here lambda = 0 converges too — for three of the four solvers — so at this step size Float32 is not regularization-limited at all.\nThe OGA dictionary size (dict_amount) has little effect on accuracy here: a few hundred candidate neurons match the reference's several hundred thousand, while being markedly faster. The dictionary is assembled in double precision, so it neither limits nor rescues the reduced-precision runs.","title":"Discussion"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Results-table","page":"Harmonic Oscillator","text":"markdown_table(summary_table(df; panelcol = :regularization))","title":"Results table"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Coarse-time-step-(Δt-1.0)","page":"Harmonic Oscillator","text":"The same benchmark with a ten-times-larger step (still ten steps, so (010)).\n\nAt this step size Float32 shows how indirectly lambda acts once the seed is the binding constraint: it converges at rung 4 only — and there for all four solvers, with the same residual 55 times 10^-6 — while every other rung, and lambda = 0, raises the OGA SingularException described above. That is not a sweet spot in lambda. The seed is re-run at every step from the previous step's solution, so a different lambda perturbs the trajectory that the next seed is built from, and which value happens to keep the Gram matrix full-rank for all ten steps is incidental. Read it as \"Float32 is marginal here\", not as \"16sqrtvarepsilon is optimal here\".\n\nspec1 = harmonic_oscillator_lode_spec(timespan = (0.0, 10.0), timestep = 1.0)\ndf1   = cached_sweep(\"nonlinear_harmonic_oscillator_dt1.0\") do\n    run_nonlinear_benchmark(spec1; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\n\nnothing # hide\n\nplot_convergence(df1; panelcol = :regularization, title = \"Harmonic Oscillator (LODE, Δt = 1.0)\")\n\nplot_accuracy(df1; panelcol = :regularization)\n\nmarkdown_table(summary_table(df1; panelcol = :regularization))","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"nonlinear_harmonic_oscillator/#Large-time-step-(Δt-10.0)","page":"Harmonic Oscillator","text":"With Delta t = 100 the ten steps span (0100) — many oscillation periods per step, a demanding test for the network representation:\n\nspec10 = harmonic_oscillator_lode_spec(timespan = (0.0, 100.0), timestep = 10.0)\ndf10   = cached_sweep(\"nonlinear_harmonic_oscillator_dt10.0\") do\n    run_nonlinear_benchmark(spec10; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\n\nnothing # hide\n\nplot_convergence(df10; panelcol = :regularization, title = \"Harmonic Oscillator (LODE, Δt = 10.0)\")\n\nplot_accuracy(df10; panelcol = :regularization)\n\nmarkdown_table(summary_table(df10; panelcol = :regularization))","title":"Large time step (Δt = 10.0)"},{"category":"section","location":"nonlinear_pendulum/#Pendulum-(Nonlinear-Integrator)","page":"Pendulum","text":"The mathematical pendulum benchmarked with the NonLinear_OneLayer_GML integrator (see Harmonic Oscillator (Nonlinear Integrator) for the network setup and the meaning of the regularization sweep). GeometricProblems.Pendulum provides no Lagrangian (lodeproblem) form, so its two-dimensional phase-space iodeproblem is used — the state is q = [angle, momentum], and the energy H(textangle textmomentum) is evaluated from both components. The pendulum is nonlinear, so it exercises the solves more than the harmonic oscillator. No closed-form solution is used; accuracy is assessed through the energy drift.\n\nThe figures panel by the regularization factor lambda — a rung of the sqrtvarepsilon(T) ladder, so one panel is a different shift at each precision; solver configurations are on the x-axis and precisions are distinguished by colour. Results are shown at Delta t = 01 and repeated for Delta t = 10 and Delta t = 100 (ten steps each).\n\nusing SolverBenchmark\n\nspec = pendulum_lode_spec(timespan = (0.0, 1.0), timestep = 0.1)\ndf   = cached_sweep(\"nonlinear_pendulum_dt0.1\") do\n    run_nonlinear_benchmark(spec; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Pendulum (Nonlinear Integrator)"},{"category":"section","location":"nonlinear_pendulum/#Convergence","page":"Pendulum","text":"plot_convergence(df; panelcol = :regularization, title = \"Pendulum (nonlinear)\")","title":"Convergence"},{"category":"section","location":"nonlinear_pendulum/#Nonlinear-iterations","page":"Pendulum","text":"plot_iterations(df; panelcol = :regularization)","title":"Nonlinear iterations"},{"category":"section","location":"nonlinear_pendulum/#Run-time","page":"Pendulum","text":"plot_runtime(df; panelcol = :regularization)","title":"Run time"},{"category":"section","location":"nonlinear_pendulum/#Energy-drift","page":"Pendulum","text":"plot_energy_drift(df; panelcol = :regularization)","title":"Energy drift"},{"category":"section","location":"nonlinear_pendulum/#Discussion","page":"Pendulum","text":"As for the harmonic oscillator, a nonzero regularization factor is required, and any rung will do. At Float64, lambda = 0 converges for no solver; every rung converges for all four, in approx 23 iterations per step, conserving the energy to 805 times 10^-8 — to three significant figures the same drift at every rung, across a ladder spanning a factor of a thousand.\nThe pendulum's nonlinearity means the solve needs a few more iterations per step than the (linear) oscillator.\nFloat32 converges from rung 2 up (23/28): all four solvers at rungs 2–6, but three of four at lambda = 0 and none at rung 1. A rung that is worse than no regularization at all is the signature of the seed, not the solver — the OGA guess is rebuilt from the previous step's solution, so lambda perturbs the trajectory it is built from and can push a later step's Gram matrix into rank deficiency. See Harmonic Oscillator (Nonlinear Integrator) for that mechanism.\nBoth 16-bit formats fail at every rung (0/28 each, against 23/28 at Float32 and 24/28 at Float64). The exception again comes from the OGA initial guess's Gram solve, not from the Newton Jacobian, so regularization_factor cannot reach it.","title":"Discussion"},{"category":"section","location":"nonlinear_pendulum/#Results-table","page":"Pendulum","text":"markdown_table(summary_table(df; panelcol = :regularization))","title":"Results table"},{"category":"section","location":"nonlinear_pendulum/#Coarse-time-step-(Δt-1.0)","page":"Pendulum","text":"spec1 = pendulum_lode_spec(timespan = (0.0, 10.0), timestep = 1.0)\ndf1   = cached_sweep(\"nonlinear_pendulum_dt1.0\") do\n    run_nonlinear_benchmark(spec1; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\nnothing # hide\n\nplot_convergence(df1; panelcol = :regularization, title = \"Pendulum (nonlinear, Δt = 1.0)\")\n\nplot_energy_drift(df1; panelcol = :regularization)\n\nmarkdown_table(summary_table(df1; panelcol = :regularization))","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"nonlinear_pendulum/#Large-time-step-(Δt-10.0)","page":"Pendulum","text":"spec10 = pendulum_lode_spec(timespan = (0.0, 100.0), timestep = 10.0)\ndf10   = cached_sweep(\"nonlinear_pendulum_dt10.0\") do\n    run_nonlinear_benchmark(spec10; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\nnothing # hide\n\nplot_convergence(df10; panelcol = :regularization, title = \"Pendulum (nonlinear, Δt = 10.0)\")\n\nmarkdown_table(summary_table(df10; panelcol = :regularization))","title":"Large time step (Δt = 10.0)"},{"category":"section","location":"nonlinear_double_pendulum/#Double-Pendulum-(Nonlinear-Integrator)","page":"Double Pendulum","text":"The double pendulum benchmarked with the NonLinear_OneLayer_GML integrator (see Harmonic Oscillator (Nonlinear Integrator) for the network setup and the regularization sweep). It is built as a two-dimensional lodeproblem; being chaotic and strongly nonlinear, it is a demanding test for the implicit network solve. Its Hamiltonian depends on both q and p, so the energy drift is evaluated from the full state. No closed-form solution exists.\n\nThe figures panel by the regularization factor lambda — a rung of the sqrtvarepsilon(T) ladder, so one panel is a different shift at each precision; solver configurations are on the x-axis and precisions are distinguished by colour. Results are shown at Delta t = 01 and repeated for Delta t = 10 and Delta t = 100 (ten steps each).\n\nusing SolverBenchmark\n\nspec = double_pendulum_lode_spec(timespan = (0.0, 1.0), timestep = 0.1)\ndf   = cached_sweep(\"nonlinear_double_pendulum_dt0.1\") do\n    run_nonlinear_benchmark(spec; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Double Pendulum (Nonlinear Integrator)"},{"category":"section","location":"nonlinear_double_pendulum/#Convergence","page":"Double Pendulum","text":"plot_convergence(df; panelcol = :regularization, title = \"Double Pendulum (nonlinear)\")","title":"Convergence"},{"category":"section","location":"nonlinear_double_pendulum/#Nonlinear-iterations","page":"Double Pendulum","text":"plot_iterations(df; panelcol = :regularization)","title":"Nonlinear iterations"},{"category":"section","location":"nonlinear_double_pendulum/#Run-time","page":"Double Pendulum","text":"plot_runtime(df; panelcol = :regularization)","title":"Run time"},{"category":"section","location":"nonlinear_double_pendulum/#Energy-drift","page":"Double Pendulum","text":"plot_energy_drift(df; panelcol = :regularization)","title":"Energy drift"},{"category":"section","location":"nonlinear_double_pendulum/#Discussion","page":"Double Pendulum","text":"Regularization is again decisive, and again flat across the ladder: at Float64, Backtracking and StrongWolfe converge at every one of the six rungs and at none of them for lambda = 0, taking approx 15 iterations per step with an energy drift of 211 times 10^-4 that is identical to three significant figures at every rung. DogLeg (a trust-region method) is the most robust here — it is the only configuration that makes progress even at lambda = 0, and Newton/Static converges nowhere at any rung. That is the clearest ordering of the four solver configurations anywhere in the study.\nThe chaotic dynamics give a larger energy drift than the integrable examples, and the achievable step size is smaller — at Delta t = 10 and above the solve struggles.\nOnly Float64 converges at all (19/28). Float32, Float16 and BFloat16 fail at every rung, and scaling lambda to the precision does not change that: the exception is raised in the OGA initial guess's Gram solve, which runs before the Newton iteration regularization_factor acts on — see Harmonic Oscillator (Nonlinear Integrator).","title":"Discussion"},{"category":"section","location":"nonlinear_double_pendulum/#Results-table","page":"Double Pendulum","text":"markdown_table(summary_table(df; panelcol = :regularization))","title":"Results table"},{"category":"section","location":"nonlinear_double_pendulum/#Coarse-time-step-(Δt-1.0)","page":"Double Pendulum","text":"spec1 = double_pendulum_lode_spec(timespan = (0.0, 10.0), timestep = 1.0)\ndf1   = cached_sweep(\"nonlinear_double_pendulum_dt1.0\") do\n    run_nonlinear_benchmark(spec1; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\nnothing # hide\n\nplot_convergence(df1; panelcol = :regularization, title = \"Double Pendulum (nonlinear, Δt = 1.0)\")\n\nmarkdown_table(summary_table(df1; panelcol = :regularization))","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"nonlinear_double_pendulum/#Large-time-step-(Δt-10.0)","page":"Double Pendulum","text":"spec10 = double_pendulum_lode_spec(timespan = (0.0, 100.0), timestep = 10.0)\ndf10   = cached_sweep(\"nonlinear_double_pendulum_dt10.0\") do\n    run_nonlinear_benchmark(spec10; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\nnothing # hide\n\nplot_convergence(df10; panelcol = :regularization, title = \"Double Pendulum (nonlinear, Δt = 10.0)\")\n\nmarkdown_table(summary_table(df10; panelcol = :regularization))","title":"Large time step (Δt = 10.0)"},{"category":"section","location":"internals/#Internals","page":"Internals","text":"How the harness is put together, and the things about the Geometric* stack that are not obvious from its interfaces. This page is for extending the package — adding a problem, an integrator or a metric. See Low-Precision Support for everything specific to the 16-bit formats and Maintenance for the build and CI.","title":"Internals"},{"category":"section","location":"internals/#What-the-package-does","page":"Internals","text":"It benchmarks the nonlinear solvers of SimpleSolvers.jl — Newton with six line searches, DogLeg and Picard — as they are used inside implicit integrators, on example problems from GeometricProblems.jl. Every run records convergence, iterations per step, run time, residual, energy drift and, where an analytic solution exists, accuracy. There are two experiment sets — implicit midpoint, and NonLinear_OneLayer_GML from NonlinearIntegrators.jl; the Home page gives the grid each one sweeps.\n\nNothing in the harness knows which problem or integrator it is running, so both sets share the same machinery and new examples are cheap to add. That is the design constraint worth preserving when extending it.","title":"What the package does"},{"category":"section","location":"internals/#Code-map","page":"Internals","text":"File Contents\nsrc/problems.jl ProblemSpec and the implicit-midpoint specs. Oscillator and pendulum use odeproblem, Lotka–Volterra 2d/4d iodeproblem, double pendulum and Toda lattice (N = 16) hodeproblem.\nsrc/configurations.jl SolverConfig/InitialGuessConfig/RegularizationConfig, solver_label/precision_label, the scaled_regularization ladder, and the default grid.\nsrc/benchmark.jl Solver options, the quiet logger, run_case/run_benchmark.\nsrc/nonlinear.jl The second experiment set: LODE specs, activation factories (relu_k/gelu/elu), nonlinear_onelayer_method, run_nonlinear_case/run_nonlinear_benchmark.\nsrc/cache.jl cached_sweep, the seam that lets the documentation build compute one page's sweeps per CI job — see Documentation.\nsrc/bfloat16.jl The BFloat16 compatibility layer — see Low-Precision Support.\nsrc/plots.jl CairoMakie helpers, summary_table, markdown_table.\nscripts/ midpoint_analysis.jl/nonlinear_analysis.jl shared runners, one script per example, run_all.jl (everything in these docs), and the standalone nonlinear_activation_study.jl/f_abstol_study.jl. All write CSV and figures to results/ (gitignored) with Delta t embedded in the file names.\n\nA problem is described entirely by a ProblemSpec: a name, a builder(T) returning the problem at precision T, an energy closure, an optional analytic reference, and the residual-tolerance factor. That is the whole extension point.","title":"Code map"},{"category":"section","location":"internals/#Driving-the-integrator-by-hand","page":"Internals","text":"Solver statistics are not returned by integrate. To read them, run_case steps the integrator itself, using the low-level API from GeometricIntegratorsBase — solutionstep, solverstate, current — and reads SimpleSolvers.iteration_number and residuals after every time step.\n\nThose functions are not re-exported by GeometricIntegrators, so the package imports the base module under an alias (import GeometricIntegratorsBase as GIB) and GIB.current in particular must stay qualified.\n\nThis is also why every metric is per-run rather than per-step: the harness aggregates as it steps, keeping one row per configuration.","title":"Driving the integrator by hand"},{"category":"section","location":"internals/#Errors-are-recorded,-not-raised","page":"Internals","text":"run_case and run_nonlinear_case catch every exception and record a non-converged row, so that one diverging configuration cannot abort a 96-run sweep. run_nonlinear_benchmark extends the same treatment to the once-per-precision network build.\n\nThe cost of that robustness is worth stating plainly:\n\nwarning: A missing method looks exactly like non-convergence\nBecause any exception becomes a non-converged row, an unimplemented method, a thrown ArgumentError and a genuinely diverging solve are indistinguishable in the results. Before writing up any new failure, re-run that single case with quiet = false and read the exception. Every gap in the BFloat16 layer first presented as \"BFloat16 does not converge on this problem\".","title":"Errors are recorded, not raised"},{"category":"section","location":"internals/#Solver-options","page":"Internals","text":"Options are merged over default_options(method, problem), so _solver_options lists only what it changes.\n\nThe framework f_abstol default is max(8, solversize(method, problem)) * eps(T), but measured it is 8 eps(T) for every problem here — including the Toda lattice at N = 16, whose 32 unknowns would otherwise raise it. GeometricIntegrators declares its solversize methods as solversize(problem, method) while GeometricIntegratorsBase's default_options calls solversize(method, problem), so the hodeproblems fall through to the generic solversize(::GeometricMethod, ::GeometricProblem) = 0. Do not assume the size-proportional default is active; f_abstol_factor is measured against 8 eps(T).\nThe \"Solver took N iterations\" warning is gated by warn_iterations, not verbosity. The harness sets both to 0.\nDogLeg and Picard reject the linesearch keyword — only Newton accepts it. Hence the linesearch === nothing branch in SolverConfig.","title":"Solver options"},{"category":"section","location":"internals/#Constructing-problems-at-a-given-precision","page":"Internals","text":"Line-search constructors are precision-typed (Backtracking(T), Bisection(T), …), which is why SolverConfig stores a callable T -> LinesearchMethod rather than an instance.\nProblem constructors take no ::Type{T} argument. Precision comes from the element type of q₀/p₀, so a spec must also pass parameters = Module.default_parameters(T) — otherwise the parameters stay Float64 and the run silently mixes precisions.\nhamiltonian requires params on every method: hamiltonian(t, q, params) for the q-only forms (harmonic oscillator, Lotka–Volterra) and hamiltonian(t, q, p, params) for the double pendulum and Toda lattice (which also takes a trailing N). There is no parameterless fallback.","title":"Constructing problems at a given precision"},{"category":"section","location":"internals/#quiet-true-filters,-it-does-not-muffle","page":"Internals","text":"quiet = true installs a filtering logger, not a NullLogger. verbosity = 0 already silences the benchmarked solver, so the filter only has to drop the two sources that no solver option reaches:\n\nGeometricIntegratorsBase's HermiteExtrapolation \"history identical\" warning — its nowarn keyword is not threaded through GeometricIntegrator.\nThe inner integrate(tem_ode, ImplicitMidpoint()) that NonLinear_OneLayer_GML runs once per step to seed its IntegratorExtrapolation. It is called with no options, so it uses SimpleSolvers' defaults (verbosity = 1, warn_iterations = 1000, Backtracking) no matter how the outer solver is configured.\n\nForwarding options to that inner call upstream would let quiet be retired for the nonlinear sweep.","title":"quiet = true filters, it does not muffle"},{"category":"section","location":"internals/#Plot-and-table-conventions","page":"Internals","text":"Display order is fixed by constants in src/plots.jl, not by row order. _PRECISION_ORDER (BFloat16, Float16, Float32, Float64) and _INITIAL_GUESS_ORDER (NoInitialGuess, HermiteExtrapolation, MidpointExtrapolation) drive comparison_figure, plot_convergence and summary_table.\nThe precision axis is filtered against _PRECISION_ORDER by string equality, and unrecognised values are silently dropped — no error, just missing bars. This is why the precision column is written with precision_label (i.e. nameof(T)) rather than string(T): Julia renders a type module-qualified whenever its module is not visible from Main, so string(BFloat16) is \"BFloat16s.BFloat16\" inside a Documenter @example sandbox.\nThe regularization panels are labelled by ladder rung, not by value, and ordered by _REGULARIZATION_ORDER. The value behind a rung depends on the precision (see How the regularization factor scales), so a value label would not be shared across the precisions a single DataFrame holds; the value and its exponent travel in the regularization_factor/regularization_exponent columns instead. _PANEL_ORDER concatenates the initial-guess and regularization orders, so one default serves both experiment sets and no call site has to name the order it wants.","title":"Plot and table conventions"},{"category":"section","location":"pendulum/#Pendulum","page":"Pendulum","text":"The mathematical pendulum (ddotq = -sin q) is nonlinear, so the implicit midpoint equations require genuine nonlinear iterations at every time step. This example therefore differentiates the solvers and line searches more strongly than the Harmonic Oscillator.\n\nThe benchmark below is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/midpoint_pendulum.jl for accurate BenchmarkTools measurements. The results are shown first for the standard step Delta t = 01 and then repeated for a coarse step Delta t = 10 (see Coarse time step (Δt = 1.0)).\n\nusing SolverBenchmark\n\nspec = pendulum_spec(timespan = (0.0, 100.0), timestep = 0.1)\ndf   = cached_sweep(\"pendulum_dt0.1\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Pendulum"},{"category":"section","location":"pendulum/#Convergence","page":"Pendulum","text":"plot_convergence(df; title = \"Pendulum\")","title":"Convergence"},{"category":"section","location":"pendulum/#Nonlinear-iterations","page":"Pendulum","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df)","title":"Nonlinear iterations"},{"category":"section","location":"pendulum/#Run-time","page":"Pendulum","text":"plot_runtime(df)","title":"Run time"},{"category":"section","location":"pendulum/#Energy-drift","page":"Pendulum","text":"Drift of the conserved energy H = p^2(2ml^2) + m g l cos q:\n\nplot_energy_drift(df)","title":"Energy drift"},{"category":"section","location":"pendulum/#Discussion","page":"Pendulum","text":"The problem is nonlinear, so Newton needs genuine iterations — about 2 per step at Float64 with Δt = 0.1. The robust line searches (Static, Backtracking, Bisection, StrongWolfe) and DogLeg again behave almost identically, so in this mildly nonlinear regime the line search barely affects the iteration count.\nEvery configuration converges at Float16 and above — 72 of the 96 runs. Newton/Quadratic converges throughout, as it does on the Harmonic Oscillator.\nBFloat16 converges only with NoInitialGuess (8/24). As for the Harmonic Oscillator, this is the BFloat16 time grid rather than the solver: at Delta t = 01 over (0 100) consecutive times collide, and the extrapolating initial guesses cannot difference them. At the coarse step it recovers to 23/24.\nPicard needs roughly 8 iterations per step and is, as for the harmonic oscillator, the most sensitive to the initial guess (MidpointExtrapolation fewest, NoInitialGuess most).\nEnergy drift scales with precision just as for the Harmonic Oscillator.","title":"Discussion"},{"category":"section","location":"pendulum/#Results-table","page":"Pendulum","text":"markdown_table(summary_table(df))","title":"Results table"},{"category":"section","location":"pendulum/#pendulum_dt1","page":"Pendulum","text":"With a ten times larger step the equations become more strongly nonlinear, which stresses the solvers noticeably more than the Delta t = 01 results above: Newton's iteration count rises to ≈ 3.3 per step and Picard needs ≈ 30. Every configuration still converges.\n\nspec1 = pendulum_spec(timespan = (0.0, 100.0), timestep = 1.0)\ndf1   = cached_sweep(\"pendulum_dt1.0\") do\n    run_benchmark(spec1; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"pendulum/#Convergence-2","page":"Pendulum","text":"plot_convergence(df1; title = \"Pendulum (Δt = 1.0)\")","title":"Convergence"},{"category":"section","location":"pendulum/#Nonlinear-iterations-2","page":"Pendulum","text":"plot_iterations(df1)","title":"Nonlinear iterations"},{"category":"section","location":"pendulum/#Run-time-2","page":"Pendulum","text":"plot_runtime(df1)","title":"Run time"},{"category":"section","location":"pendulum/#Energy-drift-2","page":"Pendulum","text":"plot_energy_drift(df1)","title":"Energy drift"},{"category":"section","location":"pendulum/#Results-table-2","page":"Pendulum","text":"markdown_table(summary_table(df1))","title":"Results table"},{"category":"section","location":"api/#API","page":"API","text":"The benchmark harness is intentionally decoupled from any particular problem or integrator, so new examples and integrators can be added easily. A problem is described by a ProblemSpec; the sweep is defined by a list of SolverConfigs and InitialGuessConfigs and executed by run_benchmark; the results are post-processed with summary_table and the plotting helpers.\n\n","title":"API"},{"category":"type","location":"api/#SolverBenchmark.InitialGuessConfig","page":"API","text":"InitialGuessConfig\n\nAn initial-guess (extrapolation) configuration for the integrator.\n\nFields\n\nname::String: label of the initial guess.\nbuild: callable () -> initial guess, returning an Extrapolation or InitialGuess instance.\n\n\n\n\n\n","title":"SolverBenchmark.InitialGuessConfig"},{"category":"type","location":"api/#SolverBenchmark.ProblemSpec","page":"API","text":"ProblemSpec\n\nSpecification of an example problem to be benchmarked. It bundles everything the benchmark harness needs to build and assess a problem at an arbitrary floating point precision T, keeping the harness itself independent of any particular problem from GeometricProblems.jl.\n\nFields\n\nname::String: human readable problem name (used for labelling and file names).\nbuilder: callable T -> problem returning an ODEProblem at precision T.\nenergy: callable (t, q, p, params) -> H computing the conserved energy / invariant from the positions q and momenta p. Used as an accuracy proxy (energy drift). p is nothing for problems whose solution carries no separate momentum (an ODEProblem state already bundles both into q); Hamiltonians that depend on positions alone simply ignore the argument.\nreference: callable (t, x₀, params) -> x giving the analytic solution at time t, or nothing when no closed-form solution is available.\nf_abstol_factor::Float64: the nonlinear solver's absolute residual tolerance is f_abstol_factor * eps(T). Defaults to 8 (the integrator's own default). Larger-scale Hamiltonian systems whose residual floor sits well above 8 eps(T) (e.g. the double pendulum, whose forces are O(g m l)) need a looser tolerance, otherwise the solver iterates against an unreachable target and is spuriously reported as non-converged.\n\n\n\n\n\n","title":"SolverBenchmark.ProblemSpec"},{"category":"type","location":"api/#SolverBenchmark.RegularizationConfig","page":"API","text":"RegularizationConfig\n\nA regularization_factor configuration for the nonlinear solver: the Levenberg–Marquardt-style shift SimpleSolvers adds to the Newton Jacobian diagonal before factorizing it.\n\nFields\n\nname::String: panel label of the configuration (\"λ = 0\", \"λ rung 1\", …).\nrung::Union{Int,Nothing}: position on the precision-scaled ladder, or nothing for a configuration whose value is the same at every precision.\nfactor::Function: callable T -> value, the shift at working precision T.\n\nThe value is a function of T, not a number. The shift has to be scaled to the precision it protects: 1e-7 is a meaningful nudge to a Float64 Jacobian and pure noise to a Float16 one, whose own √eps is already 0.03. See scaled_regularization for the ladder and nonlinear_regularization_factors for the list that is swept.\n\n\n\n\n\n","title":"SolverBenchmark.RegularizationConfig"},{"category":"method","location":"api/#SolverBenchmark.RegularizationConfig-Tuple{Real}","page":"API","text":"RegularizationConfig(λ::Real)\n\nA configuration holding the same factor λ at every precision, labelled by its value. This is how the λ = 0 control of the nonlinear sweep is built, and the convenient form for a one-off investigation at a hand-picked value.\n\n\n\n\n\n","title":"SolverBenchmark.RegularizationConfig"},{"category":"type","location":"api/#SolverBenchmark.SolverConfig","page":"API","text":"SolverConfig\n\nA single nonlinear-solver configuration to be benchmarked: a solver method from SimpleSolvers.jl together with an optional line search.\n\nFields\n\nsolver_name::String: name of the solver method (e.g. \"Newton\").\nlinesearch_name::String: name of the line search, or \"\" if none applies.\nsolver: a SimpleSolvers.NonlinearSolverMethod instance (e.g. Newton()).\nlinesearch: a callable T -> LinesearchMethod (line-search constructors are precision-typed, e.g. Backtracking(T)), or nothing.\n\nNewton supports a line search; DogLeg and Picard do not (they must be constructed with linesearch = nothing).\n\n\n\n\n\n","title":"SolverBenchmark.SolverConfig"},{"category":"type","location":"api/#SolverBenchmark.SweepNotSelected","page":"API","text":"SweepNotSelected(key)\n\nThrown by cached_sweep for a sweep that is neither cached nor selected by selected_sweeps — the other sweeps of a page whose job was given one of them.\n\nA job that raises this is doing its job: it computed its own sweep and declined the rest. Documenter is called with warnonly for such a build, so the blocks that go on to use the missing DataFrame are reported as warnings and the build still succeeds, which is all that is wanted from it — the rendered output is discarded and only the written CSVs are kept.\n\n\n\n\n\n","title":"SolverBenchmark.SweepNotSelected"},{"category":"method","location":"api/#SolverBenchmark.cached_sweep-Tuple{Any, AbstractString}","page":"API","text":"cached_sweep(compute, key)\n\nReturn the benchmark DataFrame for key: read from sweep_cache_dir if it holds <key>.csv, otherwise computed by compute() and written there. Written for do block syntax:\n\ndf = cached_sweep(\"harmonic_oscillator_dt0.1\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nWith no cache directory configured this is exactly compute(), so a local julia --project=docs docs/make.jl runs every sweep just as it would without the cache. The documentation workflow sets the directory, computes each page's sweeps in its own job, and hands the CSVs to the job that renders the site.\n\nThe round trip through CSV is lossless for everything the plots and summary_table read: missing comes back as missing, and a column that is entirely missing (accuracy on a problem with no analytic reference) comes back as Missing, which is what drop_empty and any(!ismissing, ...) already expect.\n\nThrows SweepNotSelected for a key that is neither cached nor listed in selected_sweeps, which is how a job computes one of a page's sweeps and declines the others.\n\n\n\n\n\n","title":"SolverBenchmark.cached_sweep"},{"category":"method","location":"api/#SolverBenchmark.comparison_figure-Tuple{DataFrames.DataFrame, Symbol}","page":"API","text":"comparison_figure(df, valcol; ylabel, yscale = identity, title = \"\",\n                  converged_only = false, panelcol = :initial_guess,\n                  panel_order = _PANEL_ORDER)\n\nBuild a CairoMakie.Figure comparing metric valcol across the benchmarked solver configurations. One panel is drawn per distinct value of panelcol (:initial_guess for the implicit-midpoint sweep, :regularization for the nonlinear-integrator sweep; panel_order gives its display order); within each panel the solver configurations are on the x-axis and the floating point precisions are distinguished by colour. Missing values (and, on logarithmic axes, non-positive values) are omitted. With converged_only = true only converged runs are shown (a missing bar/marker then means \"did not converge\").\n\n\n\n\n\n","title":"SolverBenchmark.comparison_figure"},{"category":"method","location":"api/#SolverBenchmark.default_initial_guesses-Tuple{}","page":"API","text":"default_initial_guesses()\n\nReturn the default list of InitialGuessConfigs: HermiteExtrapolation (the integrator default), MidpointExtrapolation, and NoInitialGuess (which reuses the solution of the previous time step).\n\n\n\n\n\n","title":"SolverBenchmark.default_initial_guesses"},{"category":"method","location":"api/#SolverBenchmark.default_precisions-Tuple{}","page":"API","text":"default_precisions()\n\nReturn the default tuple of floating point precisions to benchmark: (BFloat16, Float16, Float32, Float64).\n\nThe two 16-bit formats are both swept because they trade the same 16 bits differently: Float16 spends 11 bits on the significand and 5 on the exponent, BFloat16 only 8 on the significand but 8 on the exponent — the same range as Float32. Sweeping both separates a failure caused by too few digits from one caused by too little dynamic range.\n\n\n\n\n\n","title":"SolverBenchmark.default_precisions"},{"category":"method","location":"api/#SolverBenchmark.default_solver_configs-Tuple{}","page":"API","text":"default_solver_configs()\n\nReturn the default list of SolverConfigs: Newton combined with each of the six line searches (Static, Backtracking, Bisection, Quadratic, BierlaireQuadratic, StrongWolfe), plus DogLeg and Picard (which take no line search) — eight configurations in total.\n\n\n\n\n\n","title":"SolverBenchmark.default_solver_configs"},{"category":"method","location":"api/#SolverBenchmark.double_pendulum_lode_spec-Tuple{}","page":"API","text":"double_pendulum_lode_spec(; timespan = (0.0, 1.0), timestep = 0.1)\n\nReturn a ProblemSpec for the double pendulum from GeometricProblems.DoublePendulum, built as a two-dimensional lodeproblem. This is a chaotic, strongly nonlinear system, so it exercises the network solve hard. Its Hamiltonian depends on both q and p; the energy proxy is evaluated from the full (q, p) state. No closed-form solution exists.\n\n\n\n\n\n","title":"SolverBenchmark.double_pendulum_lode_spec"},{"category":"method","location":"api/#SolverBenchmark.double_pendulum_spec-Tuple{}","page":"API","text":"double_pendulum_spec(; q₀ = DoublePendulum.θ₀, p₀ = DoublePendulum.p₀,\n                       timespan = (0.0, 10.0), timestep = 0.01)\n\nReturn a ProblemSpec for the double pendulum from GeometricProblems.DoublePendulum, built as an hodeproblem (canonical Hamiltonian form). This is a chaotic, strongly nonlinear system, so it exercises the nonlinear solvers hard. Its Hamiltonian H(t, q, p) depends on both the generalized coordinates q and the momenta p, so the energy proxy is evaluated from the full (q, p) state. No closed-form solution exists; accuracy is assessed through the energy drift.\n\nThe default initial conditions and parameters are the problem's own module defaults; the native time span is (0, 10) with the standard Δt = 0.01 (a coarse Δt = 0.1 is used as a second scenario).\n\nBecause the double pendulum's forces are O(g m l), its nonlinear residual bottoms out around 10² eps(T) rather than eps(T), so the solver's residual tolerance is relaxed to 256 eps(T) (f_abstol_factor = 256); with the default 8 eps(T) even a fully solved step is reported as non-converged.\n\nThat floor is a property of the problem's scale, not of the precision: measured, the residual bottoms out at ≈200 … 260 eps(T) at every precision (BFloat16 1.2–1.5, Float16 0.19–0.25, Float32 2.8e-5–3.1e-5, Float64 5.0e-14–5.7e-14). A precision-dependent factor would therefore have nothing to compensate for, and tightening it for the 16-bit formats only turns \"converged at the floor\" into \"failed\": at f_abstol_factor = 8, BFloat16 and Float16 converge in 0 of 24 runs at Δt = 0.01, against 5 and 11 at 256. See scripts/f_abstol_study.jl.\n\nNote the flip side at low precision: 256 eps(BFloat16) is 2.0, comparable to the energy itself, so a BFloat16 row here reports convergence against a target too loose to be informative. summary_table flags such rows.\n\n\n\n\n\n","title":"SolverBenchmark.double_pendulum_spec"},{"category":"method","location":"api/#SolverBenchmark.elu-Tuple{Any}","page":"API","text":"elu(x)\n\nExponential Linear Unit with α = 1: x for x > 0, exp(x) - 1 otherwise. C¹ (both branch derivatives are 1 at the origin). The branch is written with ifelse so it does not force a Bool on the symbolic path.\n\n\n\n\n\n","title":"SolverBenchmark.elu"},{"category":"method","location":"api/#SolverBenchmark.gelu-Tuple{Any}","page":"API","text":"gelu(x)\n\nGaussian Error Linear Unit, tanh approximation 0.5 x (1 + tanh(√(2/π) (x + 0.044715 x³))). Smooth (C∞) and branch-free, so it traces cleanly through the symbolic-derivative path.\n\n\n\n\n\n","title":"SolverBenchmark.gelu"},{"category":"method","location":"api/#SolverBenchmark.harmonic_oscillator_lode_spec-Tuple{}","page":"API","text":"harmonic_oscillator_lode_spec(; timespan = (0.0, 1.0), timestep = 0.1)\n\nReturn a ProblemSpec for the harmonic oscillator built as an lodeproblem (Lagrangian form), as required by the NonLinear_OneLayer_GML integrator. The analytic solution is provided (via HarmonicOscillator.exact_solution_q), so accuracy is measured directly.\n\nThe nonlinear network solve has a residual floor well above 8 eps(T), so the solver tolerance is relaxed to 256 eps(T) (f_abstol_factor = 256); with the default 8 eps(T) even a well-solved step is reported as non-converged at Float32.\n\n\n\n\n\n","title":"SolverBenchmark.harmonic_oscillator_lode_spec"},{"category":"method","location":"api/#SolverBenchmark.harmonic_oscillator_spec-Tuple{}","page":"API","text":"harmonic_oscillator_spec(; x₀ = [0.5, 0.0], timespan = (0.0, 100.0), timestep = 0.1)\n\nReturn a ProblemSpec for the harmonic oscillator from GeometricProblems.HarmonicOscillator, integrated as an ODEProblem (ẋ₁ = x₂, ẋ₂ = -k x₁). The analytic solution is provided, so the accuracy of every run can be measured directly.\n\n\n\n\n\n","title":"SolverBenchmark.harmonic_oscillator_spec"},{"category":"method","location":"api/#SolverBenchmark.lotka_volterra_2d_spec-Tuple{}","page":"API","text":"lotka_volterra_2d_spec(; q₀ = [2.0, 1.0], timespan = (0.0, 10.0), timestep = 0.01)\n\nReturn a ProblemSpec for the 2d Lotka–Volterra system from GeometricProblems.LotkaVolterra2d, built as an iodeproblem (implicit ODE / degenerate Lagrangian form). This is a nonlinear, non-canonical Hamiltonian system; its Hamiltonian H = a₁q₁ + a₂q₂ + b₁\\log q₁ + b₂\\log q₂ depends on the positions q alone, and is used for the energy-drift accuracy metric.\n\nThe default time span/step are the problem's native (0, 10) with Δt = 0.01 (1000 steps): the coarse Δt = 0.1 used for the oscillator/pendulum is too large for this stiffer system.\n\n\n\n\n\n","title":"SolverBenchmark.lotka_volterra_2d_spec"},{"category":"method","location":"api/#SolverBenchmark.lotka_volterra_4d_spec-Tuple{}","page":"API","text":"lotka_volterra_4d_spec(; q₀ = [2.0, 1.0, 1.0, 1.0], timespan = (0.0, 10.0), timestep = 0.01)\n\nReturn a ProblemSpec for the 4d Lotka–Volterra system from GeometricProblems.LotkaVolterra4d, built as an iodeproblem. Like the 2d case it is a non-canonical Hamiltonian system with H = a·q + b·\\log q (positions only), used for the energy-drift metric. It is more strongly degenerate than the 2d system and is a demanding test for the nonlinear solvers — low precision typically fails with a singular Jacobian.\n\nUses the native (0, 10) time span with Δt = 0.01.\n\n\n\n\n\n","title":"SolverBenchmark.lotka_volterra_4d_spec"},{"category":"method","location":"api/#SolverBenchmark.markdown_table-Tuple{DataFrames.DataFrame}","page":"API","text":"markdown_table(df)\n\nRender a DataFrame as a Markdown.MD GFM table with compact formatting (missing shown as —, booleans as ✓/✗, floats in 3 significant digits). Useful in Documenter @example blocks, where the returned value is rendered as an HTML table.\n\n\n\n\n\n","title":"SolverBenchmark.markdown_table"},{"category":"method","location":"api/#SolverBenchmark.nonlinear_onelayer_method-Union{Tuple{Type{T}}, Tuple{T}} where T","page":"API","text":"nonlinear_onelayer_method(T; R = 8, S = 4, k = 3, bias_interval = [-π, π],\n                          dict_amount = NONLINEAR_DICT_AMOUNT,\n                          activation = relu_k(k),\n                          initial_guess_method = OGA1d_Legacy())\n\nConstruct a NonLinear_OneLayer_GML integrator at precision T: a one-layer network with S neurons and the given activation (default x -> max(0, x)^k, i.e. relu_k), integrated with an R-point Gauss–Legendre quadrature. The network basis and the quadrature are both built at T (the constructor requires them to share the element type), so the integration runs genuinely at the requested precision.\n\ninitial_guess_method selects the network's built-in seed; it defaults to OGA1d_Legacy() (see below). The activation-study script overrides both activation and initial_guess_method (with OGA1d()) to compare smooth activations against the ReLU baseline; the k keyword is used only by the default relu_k(k) and is ignored when an explicit activation is passed.\n\n\n\n\n\n","title":"SolverBenchmark.nonlinear_onelayer_method"},{"category":"method","location":"api/#SolverBenchmark.nonlinear_regularization_factors-Tuple{}","page":"API","text":"nonlinear_regularization_factors()\n\nReturn the RegularizationConfigs swept for the nonlinear integrator: the λ = 0 control, followed by the six rungs of the precision-scaled ladder (see scaled_regularization) — seven in total.\n\nλ = 0 is the control the rungs are read against. The network Newton system is near-singular, so the question the ladder answers is not whether a diagonal shift is needed but how large one has to be at a given precision — which is why the rungs are multiples of √eps(T) rather than absolute numbers: a shift small enough to be a nudge at Float64 falls below the rounding error of a Float16 Jacobian and cannot lift a singular one there.\n\n\n\n\n\n","title":"SolverBenchmark.nonlinear_regularization_factors"},{"category":"method","location":"api/#SolverBenchmark.nonlinear_solver_configs-Tuple{}","page":"API","text":"nonlinear_solver_configs()\n\nReturn the reduced list of SolverConfigs benchmarked for the nonlinear integrator: Newton with the Static, Backtracking and StrongWolfe line searches, plus DogLeg (which takes no line search) — four configurations.\n\n\n\n\n\n","title":"SolverBenchmark.nonlinear_solver_configs"},{"category":"method","location":"api/#SolverBenchmark.pendulum_lode_spec-Tuple{}","page":"API","text":"pendulum_lode_spec(; timespan = (0.0, 1.0), timestep = 0.1)\n\nReturn a ProblemSpec for the mathematical pendulum from GeometricProblems.Pendulum. The pendulum has no Lagrangian (lodeproblem) form in GeometricProblems, so its iodeproblem — a two-dimensional phase-space form whose state is q = [angle, momentum] — is used (the network integrator accepts any AbstractProblemIODE). The nonlinearity exercises the solvers more than the harmonic oscillator. No closed-form solution is used; accuracy is assessed through the energy drift H(q) = H(angle, momentum), evaluated from the two components of the state q.\n\n\n\n\n\n","title":"SolverBenchmark.pendulum_lode_spec"},{"category":"method","location":"api/#SolverBenchmark.pendulum_spec-Tuple{}","page":"API","text":"pendulum_spec(; x₀ = [acos(0.4), 0.0], timespan = (0.0, 100.0), timestep = 0.1)\n\nReturn a ProblemSpec for the mathematical pendulum from GeometricProblems.Pendulum, integrated as an ODEProblem. The pendulum is nonlinear, so it exercises the nonlinear solvers more than the (linear) harmonic oscillator. No closed-form solution is used; accuracy is assessed through the energy drift of the Hamiltonian H = p²/(2ml²) + m g l cos(q).\n\n\n\n\n\n","title":"SolverBenchmark.pendulum_spec"},{"category":"method","location":"api/#SolverBenchmark.plot_accuracy-Tuple{DataFrames.DataFrame}","page":"API","text":"plot_accuracy(df; title = \"\")\n\nComparison of the maximum error against the analytic solution (logarithmic axis). Only meaningful for problems that provide a reference solution.\n\n\n\n\n\n","title":"SolverBenchmark.plot_accuracy"},{"category":"method","location":"api/#SolverBenchmark.plot_convergence-Tuple{DataFrames.DataFrame}","page":"API","text":"plot_convergence(df; title = \"\", panelcol = :initial_guess,\n                 panel_order = _PANEL_ORDER)\n\nOverview of convergence across the whole grid: one panel per distinct value of panelcol (:initial_guess for the implicit-midpoint sweep, :regularization for the nonlinear-integrator sweep), with solver configurations on the x-axis and precisions on the y-axis. Green cells converged, red cells did not.\n\n\n\n\n\n","title":"SolverBenchmark.plot_convergence"},{"category":"method","location":"api/#SolverBenchmark.plot_energy_drift-Tuple{DataFrames.DataFrame}","page":"API","text":"plot_energy_drift(df; title = \"\")\n\nComparison of the energy (invariant) drift |H(t_end) - H(t_0)| (logarithmic axis).\n\n\n\n\n\n","title":"SolverBenchmark.plot_energy_drift"},{"category":"method","location":"api/#SolverBenchmark.plot_iterations-Tuple{DataFrames.DataFrame}","page":"API","text":"plot_iterations(df; title = \"\")\n\nGrouped bar chart of the mean number of nonlinear-solver iterations per time step (converged runs only).\n\n\n\n\n\n","title":"SolverBenchmark.plot_iterations"},{"category":"method","location":"api/#SolverBenchmark.plot_runtime-Tuple{DataFrames.DataFrame}","page":"API","text":"plot_runtime(df; title = \"\")\n\nComparison of the integration run time (seconds, logarithmic axis; converged runs only).\n\n\n\n\n\n","title":"SolverBenchmark.plot_runtime"},{"category":"method","location":"api/#SolverBenchmark.precision_label-Union{Tuple{Type{T}}, Tuple{T}} where T","page":"API","text":"precision_label(T)\n\nThe name of the precision type T as it appears in the precision column of a benchmark DataFrame, e.g. \"Float64\" or \"BFloat16\".\n\nnameof rather than string: Julia renders a type module-qualified whenever its module is not visible from Main, so string(BFloat16) yields \"BFloat16s.BFloat16\" outside a session that did using BFloat16s — for instance inside a Documenter @example sandbox. The plotting code matches this column against _PRECISION_ORDER by string equality and silently drops rows that do not match, so the label has to be independent of where it is produced.\n\n\n\n\n\n","title":"SolverBenchmark.precision_label"},{"category":"method","location":"api/#SolverBenchmark.regularization_exponent-Tuple{Type{Float64}, Integer}","page":"API","text":"regularization_exponent(T, rung)\n\nThe exponent k for which rung of the regularization ladder is 2^k √eps(T). Float64 uses (2, 4, 6, 8, 10, 12); every other precision uses (1, 2, 3, 4, 5, 6).\n\n\n\n\n\n","title":"SolverBenchmark.regularization_exponent"},{"category":"method","location":"api/#SolverBenchmark.regularization_label-Tuple{Any}","page":"API","text":"regularization_label(λ)\n\nCompact panel label for a fixed regularization factor: \"λ = 0\", \"λ = 1e-3\". The rungs of the precision-scaled ladder are labelled by position instead — their numeric value depends on the precision, and a benchmark DataFrame holds all four at once (see scaled_regularization).\n\n\n\n\n\n","title":"SolverBenchmark.regularization_label"},{"category":"method","location":"api/#SolverBenchmark.relu_k-Tuple{Integer}","page":"API","text":"relu_k(k)\n\nReturn the rectified-power activation x -> max(0, x)^k (the benchmark's default, with k = 3). This is the ridge activation OGA's ±1-weight dictionary is built for; see gelu/elu for smooth alternatives.\n\n\n\n\n\n","title":"SolverBenchmark.relu_k"},{"category":"method","location":"api/#SolverBenchmark.run_benchmark-Tuple{ProblemSpec}","page":"API","text":"run_benchmark(spec; method = ImplicitMidpoint(), precisions = default_precisions(),\n              solver_configs = default_solver_configs(),\n              initial_guesses = default_initial_guesses(),\n              timing = :quick, max_iterations = 100, verbose = true, quiet = false)\n\nRun the full benchmark grid for one problem spec and return the results as a DataFrame, one row per (precision × solver configuration × initial guess) combination. See run_case for the meaning of timing, max_iterations and quiet. Set verbose = false to suppress the per-combination progress log.\n\n\n\n\n\n","title":"SolverBenchmark.run_benchmark"},{"category":"method","location":"api/#SolverBenchmark.run_case-Union{Tuple{T}, Tuple{ProblemSpec, Type{T}, SolverConfig, InitialGuessConfig}} where T","page":"API","text":"run_case(spec, T, scfg, igcfg; method = ImplicitMidpoint(), timing = :quick,\n         max_iterations = 100, quiet = false)\n\nRun a single benchmark combination: integrate the problem described by spec at precision T using solver configuration scfg, initial guess igcfg, and the given integrator method. Returns a NamedTuple row of metrics.\n\ntiming selects how the run time is measured:\n\n:none — do not measure run time (runtime_s is missing); the trajectory is integrated only once, which is the fastest option (used by the test suite).\n:quick — a single @elapsed (fast; used for documentation builds).\n:benchmark — BenchmarkTools.@belapsed (accurate but slow; used by scripts).\n\nmax_iterations caps the nonlinear solver's iterations per step; converging configurations use far fewer, so this mainly bounds how long divergent ones run.\n\nWith quiet = true the warning reporting a failed run is suppressed (useful in documentation builds); the failure is recorded as a non-converged row regardless.\n\nAny error during integration (e.g. a divergent solve in low precision) is caught and recorded as a non-converged row rather than aborting the whole sweep.\n\n\n\n\n\n","title":"SolverBenchmark.run_case"},{"category":"method","location":"api/#SolverBenchmark.run_nonlinear_benchmark-Tuple{ProblemSpec}","page":"API","text":"run_nonlinear_benchmark(spec; method_builder = nonlinear_onelayer_method,\n                        precisions = default_precisions(),\n                        solver_configs = nonlinear_solver_configs(),\n                        regularization_factors = nonlinear_regularization_factors(),\n                        timing = :quick, max_iterations = 1000,\n                        verbose = true, quiet = false)\n\nRun the full nonlinear-integrator benchmark grid for one LODE spec and return the results as a DataFrame, one row per (precision × solver configuration × regularization configuration). The integrator method is built once per precision via method_builder(T) and reused across the solver/regularization sweep (building the network is relatively expensive). If that build fails for a precision, the whole block is recorded as non-converged rows instead of aborting the sweep.\n\nregularization_factors may mix RegularizationConfigs with bare Reals; the latter are wrapped as fixed-value configurations.\n\n\n\n\n\n","title":"SolverBenchmark.run_nonlinear_benchmark"},{"category":"method","location":"api/#SolverBenchmark.run_nonlinear_case-Union{Tuple{T}, Tuple{ProblemSpec, Type{T}, SolverConfig, RegularizationConfig, Any}} where T","page":"API","text":"run_nonlinear_case(spec, T, scfg, reg, method; timing = :quick,\n                   max_iterations = 1000, quiet = false)\n\nRun a single nonlinear-integrator benchmark combination: integrate the LODE spec at precision T with solver configuration scfg, regularization configuration reg, and the prebuilt integrator method. Uses the integrator's own initial guess (no initialguess is passed). Returns a NamedTuple row of metrics with a regularization panel label; see run_case for the timing semantics.\n\nreg is a RegularizationConfig, so the factor is resolved at T. A bare Real is also accepted and means the same factor at every precision.\n\n\n\n\n\n","title":"SolverBenchmark.run_nonlinear_case"},{"category":"method","location":"api/#SolverBenchmark.scaled_regularization-Tuple{Integer}","page":"API","text":"scaled_regularization(rung)\n\nReturn the RegularizationConfig for rung of the precision-scaled ladder: a factor of 2^k √eps(T), with k =regularization_exponent(T, rung). Labelled \"λ rung $rung\" — by position rather than by value, so that the panel label is the same across precisions even though the value is not.\n\n\n\n\n\n","title":"SolverBenchmark.scaled_regularization"},{"category":"method","location":"api/#SolverBenchmark.selected_sweeps-Tuple{}","page":"API","text":"selected_sweeps()\n\nThe set of sweep keys this build is asked to compute, from the comma-separated SOLVERBENCHMARK_SWEEPS environment variable, or nothing when it is unset — meaning \"compute whatever is asked for\".\n\nThis is what splits a page finer than the page itself. The nonlinear pages carry three sweeps each and are the slowest jobs in the documentation build, so the workflow gives each of their time steps its own job, selecting one key per job.\n\n\n\n\n\n","title":"SolverBenchmark.selected_sweeps"},{"category":"method","location":"api/#SolverBenchmark.solver_label-Tuple{SolverConfig}","page":"API","text":"solver_label(cfg::SolverConfig)\n\nA compact label combining solver and line-search name, e.g. \"Newton/Backtracking\" or \"DogLeg\".\n\n\n\n\n\n","title":"SolverBenchmark.solver_label"},{"category":"method","location":"api/#SolverBenchmark.summary_table-Tuple{DataFrames.DataFrame}","page":"API","text":"summary_table(df; panelcol = :initial_guess, panel_order = _PANEL_ORDER,\n              drop_empty = true)\n\nReturn a tidy copy of a benchmark DataFrame for display: a selection of the most relevant columns, sorted by precision, solver and panelcol. Columns that are entirely missing (e.g. accuracy for problems without an analytic reference) are dropped when drop_empty = true.\n\npanelcol is the categorical column that distinguishes the runs beyond precision and solver (:initial_guess for the implicit-midpoint sweep, :regularization for the nonlinear-integrator sweep); panel_order gives its preferred display order (values not listed are appended in first-appearance order).\n\nA converged row whose max_residual is within a factor of 10 of the f_abstol it was solved to is marked in an at_tolerance column, so that \"converged against a target too loose to mean much\" travels with the data instead of having to be remembered. The column appears only if df carries the f_abstol column that run_case records, and with drop_empty = true only if at least one row is flagged.\n\n\n\n\n\n","title":"SolverBenchmark.summary_table"},{"category":"method","location":"api/#SolverBenchmark.sweep_cache_dir-Tuple{}","page":"API","text":"sweep_cache_dir()\n\nThe directory holding cached benchmark sweeps, taken from the SOLVERBENCHMARK_SWEEP_CACHE environment variable, or nothing when that is unset.\n\n\n\n\n\n","title":"SolverBenchmark.sweep_cache_dir"},{"category":"method","location":"api/#SolverBenchmark.toda_lattice_lode_spec-Tuple{}","page":"API","text":"toda_lattice_lode_spec(; N = 16, μ = 0.3, timespan = (0.0, 1.0), timestep = 0.1)\n\nReturn a ProblemSpec for the Toda lattice from GeometricProblems.TodaLattice, built as an N-dimensional lodeproblem (N = 16 sites, so the network integrator solves a 16-dimensional implicit system per step). Its Hamiltonian H(t, q, p, N) depends on both q and p (and on N, which the energy closure captures). No closed-form solution exists.\n\n\n\n\n\n","title":"SolverBenchmark.toda_lattice_lode_spec"},{"category":"method","location":"api/#SolverBenchmark.toda_lattice_spec-Tuple{}","page":"API","text":"toda_lattice_spec(; N = 16, μ = 0.3, timespan = (0.0, 100.0), timestep = 0.1)\n\nReturn a ProblemSpec for the Toda lattice from GeometricProblems.TodaLattice, built as an hodeproblem with N = 16 lattice sites. The full soliton example uses N = 200, which would make the implicit solves prohibitively expensive for a solver benchmark, so a modest N = 16 is used here. The lattice size is passed positionally to hodeproblem; the initial positions come from compute_initial_q(μ, N) (a bump of width μ) with zero initial momenta. Its Hamiltonian H(t, q, p, N) depends on both q and p (and on N, which the energy closure captures). No closed-form solution exists; accuracy is assessed through the energy drift.\n\nThe native time span (0, 120) is shortened to (0, 100) with the standard Δt = 0.1; a coarse Δt = 1.0 is used as a second scenario.\n\nUnlike the double pendulum this spec keeps the framework's default residual tolerance: it does not have the double pendulum's raised residual floor, so relaxing the tolerance to 256 eps(T) here buys 3 converged runs of 96 at Δt = 0.1 and 1 at Δt = 1.0 while costing one to two orders of magnitude of residual on every run that converges either way (Float64 worst case 1.8e-15 against 5.7e-14, Float32 9.5e-7 against 3.1e-5), and the extra runs it admits stop at a residual of ≈0.19 rather than ≈0.008. See scripts/f_abstol_study.jl.\n\n\n\n\n\n","title":"SolverBenchmark.toda_lattice_spec"},{"category":"section","location":"lotka_volterra_2d/#Lotka–Volterra-(2d)","page":"Lotka–Volterra (2d)","text":"The 2d Lotka–Volterra system is a non-canonical Hamiltonian system, benchmarked here in its implicit form via iodeproblem (an implicit ODE / degenerate Lagrangian). Its Hamiltonian H = a_1 q_1 + a_2 q_2 + b_1 log q_1 + b_2 log q_2 depends on the positions q alone and is used for the energy-drift metric. The system is stiffer than the oscillator and pendulum, so the native time span (0 10) with Delta t = 001 is used; the results are then repeated for a ten times coarser step Delta t = 01 (see Coarse time step (Δt = 0.1)).\n\nThe benchmark below is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/midpoint_lotka_volterra_2d.jl for accurate BenchmarkTools measurements.\n\nusing SolverBenchmark\n\nspec = lotka_volterra_2d_spec()\ndf   = cached_sweep(\"lotka_volterra_2d_dt0.01\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Lotka–Volterra (2d)"},{"category":"section","location":"lotka_volterra_2d/#Convergence","page":"Lotka–Volterra (2d)","text":"plot_convergence(df; title = \"Lotka–Volterra (2d)\")","title":"Convergence"},{"category":"section","location":"lotka_volterra_2d/#Nonlinear-iterations","page":"Lotka–Volterra (2d)","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df)","title":"Nonlinear iterations"},{"category":"section","location":"lotka_volterra_2d/#Run-time","page":"Lotka–Volterra (2d)","text":"plot_runtime(df)","title":"Run time"},{"category":"section","location":"lotka_volterra_2d/#Energy-drift","page":"Lotka–Volterra (2d)","text":"Drift of the conserved Hamiltonian:\n\nplot_energy_drift(df)","title":"Energy drift"},{"category":"section","location":"lotka_volterra_2d/#Discussion","page":"Lotka–Volterra (2d)","text":"Being an implicit, non-canonical system, the implicit midpoint equations are genuinely nonlinear: Newton (with a robust line search) and DogLeg converge in about 2 iterations per step at Float32/Float64.\nPicard never converges on this system — unlike the harmonic oscillator and pendulum (where it converged, if slowly), the fixed-point iteration diverges on the non-canonical iodeproblem formulation. It is the only solver that fails at Float32/Float64: every other configuration converges at both precisions.\nFloat16 largely fails (9/24 here, 13/24 at the coarse step): the Newton linear solve hits singular Jacobians and the trajectory diverges. Float32 (19/24) and Float64 (21/24) behave essentially identically — the achievable step accuracy is already reached at Float32.\nBFloat16 does worse than Float16, not better — 1/24 here and 7/24 at the coarse step, against 9/24 and 13/24. This is the comparison the two 16-bit formats were added for, and it identifies the cause: the failures come from near-singular Jacobians, which need significand bits, and BFloat16 trades three of them away for exponent range this problem never needs. (At Delta t = 001 the BFloat16 figure is further depressed by the time-grid collision described under Harmonic Oscillator; the coarse step is the honest comparison.)\nAs always the initial guess matters only for the solvers that iterate more; for the one- to two-iteration Newton/DogLeg runs it has little effect.","title":"Discussion"},{"category":"section","location":"lotka_volterra_2d/#Results-table","page":"Lotka–Volterra (2d)","text":"markdown_table(summary_table(df))","title":"Results table"},{"category":"section","location":"lotka_volterra_2d/#lotka_volterra_2d_dt01","page":"Lotka–Volterra (2d)","text":"The same benchmark over (0 10) with a ten times larger step. The problem becomes harder: the converging solvers need more iterations (≈ 3 per step instead of ≈ 2), while the same configurations continue to fail (Picard, and much of Float16).\n\nspec1 = lotka_volterra_2d_spec(timespan = (0.0, 10.0), timestep = 0.1)\ndf1   = cached_sweep(\"lotka_volterra_2d_dt0.1\") do\n    run_benchmark(spec1; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Coarse time step (Δt = 0.1)"},{"category":"section","location":"lotka_volterra_2d/#Convergence-2","page":"Lotka–Volterra (2d)","text":"plot_convergence(df1; title = \"Lotka–Volterra (2d, Δt = 0.1)\")","title":"Convergence"},{"category":"section","location":"lotka_volterra_2d/#Nonlinear-iterations-2","page":"Lotka–Volterra (2d)","text":"plot_iterations(df1)","title":"Nonlinear iterations"},{"category":"section","location":"lotka_volterra_2d/#Run-time-2","page":"Lotka–Volterra (2d)","text":"plot_runtime(df1)","title":"Run time"},{"category":"section","location":"lotka_volterra_2d/#Energy-drift-2","page":"Lotka–Volterra (2d)","text":"plot_energy_drift(df1)","title":"Energy drift"},{"category":"section","location":"lotka_volterra_2d/#Results-table-2","page":"Lotka–Volterra (2d)","text":"markdown_table(summary_table(df1))","title":"Results table"},{"category":"section","location":"nonlinear_toda_lattice/#Toda-Lattice-(Nonlinear-Integrator)","page":"Toda Lattice","text":"The Toda lattice (N = 16 sites) benchmarked with the NonLinear_OneLayer_GML integrator (see Harmonic Oscillator (Nonlinear Integrator) for the network setup and the regularization sweep). It is built as a 16-dimensional lodeproblem, so the network integrator solves a 16-dimensional implicit system at every step — the highest-dimensional problem in this study. Its Hamiltonian depends on both q and p; the energy drift is evaluated from the full state. No closed-form solution exists.\n\nThe figures panel by the regularization factor lambda — a rung of the sqrtvarepsilon(T) ladder, so one panel is a different shift at each precision; solver configurations are on the x-axis and precisions are distinguished by colour. Results are shown at Delta t = 01 and repeated for Delta t = 10 and Delta t = 100 (ten steps each).\n\nusing SolverBenchmark\n\nspec = toda_lattice_lode_spec(timespan = (0.0, 1.0), timestep = 0.1)\ndf   = cached_sweep(\"nonlinear_toda_lattice_dt0.1\") do\n    run_nonlinear_benchmark(spec; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Toda Lattice (Nonlinear Integrator)"},{"category":"section","location":"nonlinear_toda_lattice/#Convergence","page":"Toda Lattice","text":"plot_convergence(df; panelcol = :regularization, title = \"Toda Lattice (nonlinear)\")","title":"Convergence"},{"category":"section","location":"nonlinear_toda_lattice/#Nonlinear-iterations","page":"Toda Lattice","text":"plot_iterations(df; panelcol = :regularization)","title":"Nonlinear iterations"},{"category":"section","location":"nonlinear_toda_lattice/#Run-time","page":"Toda Lattice","text":"plot_runtime(df; panelcol = :regularization)","title":"Run time"},{"category":"section","location":"nonlinear_toda_lattice/#Energy-drift","page":"Toda Lattice","text":"plot_energy_drift(df; panelcol = :regularization)","title":"Energy drift"},{"category":"section","location":"nonlinear_toda_lattice/#Discussion","page":"Toda Lattice","text":"Despite its dimensionality the lattice is the cleanest case in the set: at Float64, all four solvers converge at all six rungs and none at lambda = 0 — 24 of 28, a perfect split on lambda  0. The 16-dimensional solve takes approx 12 iterations per step and conserves the energy to 375 times 10^-13, again indistinguishable from rung to rung across a ladder spanning a factor of a thousand. Regularization here is purely a threshold.\nRun times are higher than the low-dimensional examples (16 coupled network fits per step), which is the main cost visible here.\nOnly Float64 converges (24/28); Float32, Float16 and BFloat16 fail at every rung. Scaling lambda to the precision does not help them, because the exception is not the one lambda addresses: it is raised in the OGA initial guess's Gram solve, upstream of the Newton iteration — see Harmonic Oscillator (Nonlinear Integrator).","title":"Discussion"},{"category":"section","location":"nonlinear_toda_lattice/#Results-table","page":"Toda Lattice","text":"markdown_table(summary_table(df; panelcol = :regularization))","title":"Results table"},{"category":"section","location":"nonlinear_toda_lattice/#Coarse-time-step-(Δt-1.0)","page":"Toda Lattice","text":"spec1 = toda_lattice_lode_spec(timespan = (0.0, 10.0), timestep = 1.0)\ndf1   = cached_sweep(\"nonlinear_toda_lattice_dt1.0\") do\n    run_nonlinear_benchmark(spec1; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\nnothing # hide\n\nplot_convergence(df1; panelcol = :regularization, title = \"Toda Lattice (nonlinear, Δt = 1.0)\")\n\nmarkdown_table(summary_table(df1; panelcol = :regularization))","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"nonlinear_toda_lattice/#Large-time-step-(Δt-10.0)","page":"Toda Lattice","text":"spec10 = toda_lattice_lode_spec(timespan = (0.0, 100.0), timestep = 10.0)\ndf10   = cached_sweep(\"nonlinear_toda_lattice_dt10.0\") do\n    run_nonlinear_benchmark(spec10; timing = :quick, max_iterations = 100,\n                            verbose = false, quiet = true)\nend\nnothing # hide\n\nplot_convergence(df10; panelcol = :regularization, title = \"Toda Lattice (nonlinear, Δt = 10.0)\")\n\nmarkdown_table(summary_table(df10; panelcol = :regularization))","title":"Large time step (Δt = 10.0)"},{"category":"section","location":"toda_lattice/#Toda-Lattice","page":"Toda Lattice","text":"The Toda lattice is a nonlinear chain of particles with exponential nearest-neighbour interactions. It is built here with hodeproblem and a modest lattice size N = 16 (the full soliton example uses N = 200, which would make the implicit solves prohibitively expensive for a solver benchmark). The initial positions come from a bump of width mu = 03 with zero initial momenta. The Hamiltonian H(t q p) depends on both the coordinates and the momenta, so the energy-drift proxy is evaluated from the full (q p) state.\n\nUnlike the Double Pendulum, this problem uses the framework's default residual tolerance 8varepsilon(T): it does not have the double pendulum's raised residual floor, so relaxing the tolerance to 256varepsilon(T) here buys only 3 converged runs out of 96 at Delta t = 01 and 1 at Delta t = 10, while costing one to two orders of magnitude of residual on every run that converges either way.\n\nThe benchmark below is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/midpoint_toda_lattice.jl for accurate BenchmarkTools measurements. The results are shown first for the standard step Delta t = 01 and then repeated for a coarse step Delta t = 10 (see Coarse time step (Δt = 1.0)).\n\nusing SolverBenchmark\n\nspec = toda_lattice_spec(timespan = (0.0, 100.0), timestep = 0.1)\ndf   = cached_sweep(\"toda_lattice_dt0.1\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Toda Lattice"},{"category":"section","location":"toda_lattice/#Convergence","page":"Toda Lattice","text":"plot_convergence(df; title = \"Toda Lattice\")","title":"Convergence"},{"category":"section","location":"toda_lattice/#Nonlinear-iterations","page":"Toda Lattice","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df)","title":"Nonlinear iterations"},{"category":"section","location":"toda_lattice/#Run-time","page":"Toda Lattice","text":"plot_runtime(df)","title":"Run time"},{"category":"section","location":"toda_lattice/#Energy-drift","page":"Toda Lattice","text":"Drift of the conserved energy H(t q p) of the Toda lattice:\n\nplot_energy_drift(df)","title":"Energy drift"},{"category":"section","location":"toda_lattice/#Discussion","page":"Toda Lattice","text":"The 16-dimensional implicit solve is the most expensive of the examples, so the run-time differences between solver configurations are more pronounced.\nFloat16, Float32 and Float64 are indistinguishable in convergence (21/24 each at Delta t = 01): despite being the highest-dimensional problem of the set, the Toda lattice is well conditioned, and the three failures in each column are Picard, which never converges here.\nBFloat16 reaches only 7/24 at Delta t = 01 but 21/24 at Delta t = 10, matching the other three. The difference is not the solver but the time grid: at the finer step BFloat16 cannot represent (0 100) in increments of 0.1, so the extrapolating initial guesses fail (see Harmonic Oscillator).\nNo closed-form solution is available, so accuracy is judged solely through the energy drift, which scales with the floating point precision.","title":"Discussion"},{"category":"section","location":"toda_lattice/#Results-table","page":"Toda Lattice","text":"markdown_table(summary_table(df))","title":"Results table"},{"category":"section","location":"toda_lattice/#toda_lattice_dt1","page":"Toda Lattice","text":"With a ten times larger step the nonlinear solves per step become harder, which stresses the solvers more than the Delta t = 01 results above.\n\nspec1 = toda_lattice_spec(timespan = (0.0, 100.0), timestep = 1.0)\ndf1   = cached_sweep(\"toda_lattice_dt1.0\") do\n    run_benchmark(spec1; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"toda_lattice/#Convergence-2","page":"Toda Lattice","text":"plot_convergence(df1; title = \"Toda Lattice (Δt = 1.0)\")","title":"Convergence"},{"category":"section","location":"toda_lattice/#Nonlinear-iterations-2","page":"Toda Lattice","text":"plot_iterations(df1)","title":"Nonlinear iterations"},{"category":"section","location":"toda_lattice/#Run-time-2","page":"Toda Lattice","text":"plot_runtime(df1)","title":"Run time"},{"category":"section","location":"toda_lattice/#Energy-drift-2","page":"Toda Lattice","text":"plot_energy_drift(df1)","title":"Energy drift"},{"category":"section","location":"toda_lattice/#Results-table-2","page":"Toda Lattice","text":"markdown_table(summary_table(df1))","title":"Results table"},{"category":"section","location":"findings/#Key-Findings","page":"Key Findings","text":"A summary of what the two experiment sets actually measured, gathered in one place. Every number below comes from the raw results the per-problem pages regenerate; the figures and tables that support each claim are on those pages.\n\nMeasured 2026-08-10 against GeometricIntegrators 0.17, GeometricIntegratorsBase 0.5.1, SimpleSolvers 0.10.1, GeometricProblems 0.8.2 and EulerLagrange 0.5.1. The implicit-midpoint set is twelve sweeps (six problems × two time steps) of 96 runs each; the nonlinear set is twelve sweeps (four problems × three time steps) of 112 runs each (4 precisions × 4 solvers × 7 regularization factors).","title":"Key Findings"},{"category":"section","location":"findings/#Solvers-and-line-searches","page":"Key Findings","text":"Newton with a robust line search, and DogLeg, are the most efficient — about 1.15 iterations per step averaged over the oscillator and the pendulum, against 2.8 for Newton/Bisection and 3.4 for Picard.\nAll six line searches are comparably robust, and so is DogLeg. Over the twelve implicit-midpoint sweeps, 144 runs each:\nSolver configuration Converged\nNewton/Bisection 129 / 144\nDogLeg 117 / 144\nNewton/Static 114 / 144\nNewton/Backtracking 114 / 144\nNewton/StrongWolfe 114 / 144\nNewton/BierlaireQuadratic 113 / 144\nNewton/Quadratic 106 / 144\nPicard 44 / 144\nThe choice between line searches matters far less than the choice of solver.\nPicard is slow where it works and fails where it does not. On the Harmonic Oscillator and Pendulum it converges (22/24 each) but needs the most iterations per step; on the Lotka–Volterra iodeproblems, the Double Pendulum and the Toda Lattice it never converges (0/24 each). On the Toda lattice it is the only failure at Float32/Float64.","title":"Solvers and line searches"},{"category":"section","location":"findings/#Reading-the-residual-columns","page":"Key Findings","text":"Bisection stops at the tolerance it was asked for, where the others overshoot it. Its residual lands right at f_abstol, while the remaining line searches drive the residual one to two orders of magnitude below the requested tolerance. This is not a defect — Bisection is also the most robust of the six, so the two facts are related rather than in tension.\n\nRead the residual columns against f_abstol, not against each other. Every results table records the f_abstol each run was solved to and marks rows that stopped at that target in an at_tolerance column.\n\nThat column matters most at reduced precision. The Double Pendulum relaxes its tolerance to 256varepsilon(T) because its residual floor sits well above varepsilon(T) — measured, at approx 200 dots 260varepsilon(T) at every precision, since the floor is set by the problem's O(g m l) force scale rather than by the format. At BFloat16 that tolerance is 2.0, comparable to the energy itself, so a converged BFloat16 row there reports having met a target too loose to say much.","title":"Reading the residual columns"},{"category":"section","location":"findings/#Precision","page":"Key Findings","text":"Precision sets the achievable accuracy. On the oscillator at Delta t = 01 the energy drift is approx 7e-17, 1e-7, 4e-4 and 4e-4 for Float64, Float32, Float16 and BFloat16, while the discretization error (approx 1.5e-2 here) is precision-independent — Float32 already reaches it.\nOver all 288 implicit-midpoint runs at each precision:\nPrecision Converged\nFloat64 263 / 288\nFloat32 261 / 288\nFloat16 206 / 288\nBFloat16 121 / 288","title":"Precision"},{"category":"section","location":"findings/#The-two-16-bit-formats","page":"Key Findings","text":"Float16 and BFloat16 divide the same 16 bits differently — 11 significand bits and a 5-bit exponent against 8 and 8 — so sweeping both separates a failure caused by too few digits from one caused by too little dynamic range. They give two distinct answers, and it is worth keeping them apart.\n\nOn conditioning, the answer is \"significand, not exponent\". Where the comparison is not confounded by the time-grid effect below, BFloat16 never beats Float16: 7/24 against 13/24 on Lotka–Volterra (2d) and 6/24 against 6/24 on Lotka–Volterra (4d) at Delta t = 01, and 0/16 against 0/16 everywhere in the nonlinear set. The stiff-system and network-Jacobian failures come from near-singular factorizations, which need significand bits — and BFloat16 trades three of them away for an exponent range these problems never need.\n\nOn the time grid, BFloat16 has a limitation Float16 does not. With 8 significand bits its spacing at t = 100 is 0.5, five times a Delta t = 01 step, so the time series collides: 0:0.1:100 holds 1001 points but only 451 distinct values. HermiteExtrapolation and MidpointExtrapolation are then handed two identical times and abort for every solver, leaving only NoInitialGuess. This is a property of the time grid, not of the solver, and it reverses the usual trend — the oscillator goes from 8/24 at Delta t = 01 to 24/24 at Delta t = 10, the one place in this study where a larger step helps. Float16, with 11 significand bits, resolves both grids.\n\nOnly compare the two formats at a Delta t where the BFloat16 grid resolves, or the second effect will be mistaken for the first.\n\nnote: `BFloat16` needs a compatibility layer\nBFloat16 is not supported by this stack out of the box: Base and BFloat16s together lack rem, Integer, sincos (which recurses until the stack overflows), two-argument atan, fma and mod2pi, and NaNMath — against which GeometricProblems writes its right-hand sides — declares its NaN-returning variants for Union{Float16,Float32,Float64} only. src/bfloat16.jl fills those gaps; it is type piracy over upstream omissions and is meant to be deleted as they are fixed.","title":"The two 16-bit formats"},{"category":"section","location":"findings/#Time-step","page":"Key Findings","text":"Larger time steps increase iteration counts and cause more line-search failures — with the BFloat16 exception above, where a larger step fixes the time-grid collision.","title":"Time step"},{"category":"section","location":"findings/#Nonlinear-integrator-set","page":"Key Findings","text":"Regularization is a threshold, not a tuned value. Over the eleven measured sweeps, lambda = 0 converges 8 times out of 176 while each of the six rungs converges 23–31 times out of 176 — and the rungs are flat between themselves, across ladders spanning a factor of a thousand:\nlambda 0 rung 1 rung 2 rung 3 rung 4 rung 5 rung 6\nConverged 8 / 176 23 27 27 31 27 27\nRung 4's slight lead is not a peak worth tuning to; where the solve is regularization-limited at all, every rung fixes it, and iteration count, residual and energy drift agree to three significant figures from rung to rung. The value to reach for is any nonzero one — NonlinearIntegrators' 16sqrtvarepsilon(T) (rung 4, or rung 2 at Float64) is as good as it needs to be.\nConverged runs per sweep, out of 112. Eleven of the twelve are measured; the Toda lattice at Delta t = 100 is still running, so every aggregate below is over those eleven:\nProblem Delta t = 01 Delta t = 10 Delta t = 100\nHarmonic Oscillator (Nonlinear Integrator) 51 29 0\nPendulum (Nonlinear Integrator) 47 0 0\nDouble Pendulum (Nonlinear Integrator) 19 0 0\nToda Lattice (Nonlinear Integrator) 24 0 pending\nDelta t = 100 converges nowhere on the three problems measured so far. At Delta t = 10 only the harmonic oscillator survives, with 29 — 24 of them Float64 at every rung, 4 Float32 at rung 4 alone, and 1 at lambda = 0.\nNeither 16-bit format converges anywhere in this set — 0/28 in every sweep, for both. Float32 reaches 54/308 and Float64 116/308.\nRegularization cannot rescue the reduced-precision runs, because they do not fail where it acts. Of 308 rows each, BFloat16 and Float16 raised an exception on every one and stalled on none: not a single 16-bit run ever reached a Newton iteration that could be reported as non-convergent. The exception is a SingularException from NonlinearIntegrators.initial_params! — the G_k x_k = b Gram solve of the OGA initial guess, whose third selected neuron is already linearly dependent on its predecessors at 16 bits. That runs before the Newton solve of every step, and regularization_factor shifts only the Newton Jacobian diagonal, so no rung reaches it. Fixing it means regularizing the seed's normal equations upstream.\nWhere lambda does reach the solve it is decisive, and that is Float64 throughout plus Float32 at the fine step: Float64 stalls (rather than throws) on 95 of 308 rows, which is the failure mode a diagonal shift addresses, and every rung removes it. Float32 sits in between — 198 threw, 56 stalled.\nA rung can be worse than no regularization at all, which is the signature of the seed rather than the solver: Float32 converges for three solvers at lambda = 0 on the pendulum and for none at rung 1, and on the oscillator at Delta t = 10 only rung 4 survives. The OGA guess is rebuilt from the previous step's solution, so lambda perturbs the trajectory the next seed is built from and can push a later step's Gram matrix into rank deficiency. Read isolated survivals as marginality, not as an optimum in lambda.\nFloat32 LODE runs are as fast as Float64 ones (0.004 s on the harmonic oscillator).","title":"Nonlinear integrator set"},{"category":"section","location":"maintenance/#Maintenance","page":"Maintenance","text":"Build, dependency and CI notes for this repository.","title":"Maintenance"},{"category":"section","location":"maintenance/#Dependencies","page":"Maintenance","text":"Stdlib dependencies must be declared explicitly in Project.toml — Printf, Markdown and Logging — each with compat = \"1\". Without the compat entry Pkg.add pins them to the running Julia's stdlib version, which then conflicts with julia = \"1.11\".\nAfter adding or changing a package dependency, re-resolve the docs environment as well:\njulia --project=docs -e 'using Pkg; Pkg.resolve()'\nIt does not inherit the root project.\nNonlinearIntegrators is unregistered and comes from its GitHub main branch via a [sources] entry, which requires Julia 1.11 or newer. The same entry has to be repeated in docs/Project.toml, for the same reason.","title":"Dependencies"},{"category":"section","location":"maintenance/#Documentation","page":"Maintenance","text":"The analysis pages regenerate their figures at build time inside @example blocks, with timing = :quick, quiet = true. A change to the benchmark therefore changes the docs, and a benchmark that errors breaks the build.\nThe build is fanned out over eighteen jobs, because running all twenty-four sweeps in one job takes hours. Every sweep goes through cached_sweep, keyed by page and time step:\n \nsweep matrix job DOCS_PAGES=<page>.md julia --project=docs docs/make.jl with SOLVERBENCHMARK_SWEEP_CACHE set. Builds that one page, so only its sweeps run; the rendered output is discarded and the written CSVs are uploaded as an artifact.\ndocumenter job Downloads every artifact into one directory, builds the whole site — now all cache hits — and deploys.\nMeasured on one page: 148 s computing versus 43 s from cache, so the assembling job is dominated by the fixed Documenter overhead rather than by the benchmark.\nSOLVERBENCHMARK_SWEEPS splits a page finer than the page, naming the cache keys a job is to compute; the page's other sweeps raise SweepNotSelected and are skipped. The implicit-midpoint pages take a whole page each (two sweeps), while the nonlinear pages get one job per time step — three sweeps of 112 runs apiece, and they set the critical path of the workflow, so splitting them is what shortens it.\nAdding a sweep to a page means adding it to the matrix in .github/workflows/Documenter.yml. A key that no job claims is not an error: the documenter job computes it itself, which shows as that job taking far longer than its usual few minutes and as its collected N sweeps count falling short of 24.\npagesonly=true is what makes the split work. pages only builds the navigation; without pagesonly Documenter expands every .md under docs/src, and each sweep job would run the entire study instead of its own page. A page added to docs/src but not to PAGES is silently dropped from the build.\nA partial build passes warnonly=true, since cross-references into the omitted pages cannot resolve. The documenter job builds with warnings fatal, so a broken @ref still fails there — after every sweep has run, which is what makes it an expensive mistake. docs/make.jl deploys only when DEPLOY_DOCS is true, which the workflow sets from needs.sweep.result, so an incomplete run publishes nothing and uploads the site as an artifact instead.\nThe docs are built only by .github/workflows/Documenter.yml. CI.yml runs the tests; building the docs there too would duplicate hours of compute and race the other build for gh-pages.\n\nwarning: The documentation is built on macOS with Julia 1.11, and why\nOn Linux with Julia 1.12 (LLVM 18.1.7) the sweeps abort mid-run with a silent SIGABRT: exit 134, no exception, no stack trace, nothing on stderr even under a pty. A core dump named it:llvm::SelectionDAGISel::CannotYetSelect(llvm::SDNode*)\n  → llvm::report_fatal_error → abort()\nfrom JuliaOJIT::addModule ← jl_compile_codeinst_now ← jl_compile_method_internalLLVM's X86 backend cannot select an instruction for a DAG node while the JIT compiles a method, so it calls report_fatal_error. Because that aborts inside LLVM, it never becomes a Julia exception — hence the silence. It is an upstream code-generation bug; nothing in this repository is implicated, and the same commit passes and fails depending only on which runner it draws.That is also why it looked so erratic. It hit a different sweep each run, because the host's target features and which specialization gets compiled decide, not the benchmark. Retrying was useless (the same runner reproduced it at 66 s, then 63 s), and it never reproduced on arm64. Failures fell mostly on the newest cores — znver5, graniterapids, sapphirerapids — but not exclusively, so the microarchitecture is a tendency and the backtrace is the fact.Ruled out along the way, each by a test that actually ran: BLAS threading (OPENBLAS_NUM_THREADS=1) and OpenBLAS kernel dispatch (OPENBLAS_CORETYPE).note: `JULIA_CPU_TARGET` cannot be probed by setting it for the run\nIt does not invalidate the pkgimages, so Julia reuses the ones built for the native target and nothing is tested — while the attempt reports code generation as excluded. The tell is timing: a no-op reaches ExpandTemplates half a minute later, where a genuine precompile of this stack takes eleven minutes.So both jobs run on macos-latest with Julia 1.11: arm64 uses a different LLVM backend, and 1.11 predates this LLVM. Revisit once a Julia release carries a fixed LLVM — the pin is a workaround for an upstream bug, not a requirement of this package, which supports 1.11 upwards and is tested across all three platforms by CI.yml.\n\nDocumenter inlines figures as base64, so several figures per page comfortably exceed the default page-size limit. size_threshold (and size_threshold_warn) are raised in the Documenter.HTML block of docs/make.jl.\nA stale figure in a rendered page is worth treating as a symptom: the precision axis drops rows it does not recognise silently, so missing bars mean a label mismatch rather than a plotting bug. See Plot and table conventions.","title":"Documentation"},{"category":"section","location":"maintenance/#Re-measuring","page":"Maintenance","text":"julia --project=. scripts/run_all.jl\n\nruns both experiment sets — six implicit-midpoint problems at two time steps and four nonlinear problems at three — with BenchmarkTools timing, writing CSV and figures to results/. It takes hours, and because it measures run times, nothing else CPU-heavy should run alongside it.\n\nThen update Key Findings from the CSVs, and restate the stack versions and date at the top of that page. Two standalone studies support it:\n\nscripts/f_abstol_study.jl — the residual-tolerance comparison behind How the residual tolerance scales.\nscripts/nonlinear_activation_study.jl — smooth activations against the ReLU baseline for the nonlinear integrator.\n\nnote: Output is buffered when redirected\nJulia buffers stdout and stderr when they are not a terminal, so a long run redirected to a file shows nothing until it exits — and a run that is killed loses its progress entirely. scripts/f_abstol_study.jl flushes explicitly for this reason; add the same if you write another long-running script.","title":"Re-measuring"},{"category":"section","location":"maintenance/#CI","page":"Maintenance","text":".github/workflows/CI.yml:\n\nmacOS-latest runners are arm64 — do not force arch: x64, which makes setup-julia error. Leave arch unspecified so the runner's native architecture is used.\nPin the Julia matrix to '1' (latest stable) rather than a specific version; naming an unreleased one fails with \"Could not find a Julia version that matches …\".\nnightly is continue-on-error. It fails upstream often enough that it would otherwise take the whole workflow down with it.\nPushes to main queue rather than cancel — the concurrency group does not cancel in-progress runs. Dependabot Actions-bump PRs auto-merge and occasionally need a git rebase origin/main.","title":"CI"},{"category":"section","location":"harmonic_oscillator/#Harmonic-Oscillator","page":"Harmonic Oscillator","text":"The harmonic oscillator is a linear problem (ddotx = -kx). Newton's method therefore solves the implicit midpoint equations exactly in a single iteration, which makes this example a useful baseline: differences between the solver configurations are dominated by convergence behaviour and floating point precision rather than by nonlinearity.\n\nThe benchmark below is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/midpoint_harmonic_oscillator.jl for accurate BenchmarkTools measurements. The results are shown first for the standard step Delta t = 01 and then repeated for a coarse step Delta t = 10 (see Coarse time step (Δt = 1.0)).\n\nusing SolverBenchmark\n\nspec = harmonic_oscillator_spec(timespan = (0.0, 100.0), timestep = 0.1)\ndf   = cached_sweep(\"harmonic_oscillator_dt0.1\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Harmonic Oscillator"},{"category":"section","location":"harmonic_oscillator/#Convergence","page":"Harmonic Oscillator","text":"Which combinations reached the solver tolerance at every time step:\n\nplot_convergence(df; title = \"Harmonic Oscillator\")","title":"Convergence"},{"category":"section","location":"harmonic_oscillator/#Nonlinear-iterations","page":"Harmonic Oscillator","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df)","title":"Nonlinear iterations"},{"category":"section","location":"harmonic_oscillator/#Run-time","page":"Harmonic Oscillator","text":"plot_runtime(df)","title":"Run time"},{"category":"section","location":"harmonic_oscillator/#Energy-drift","page":"Harmonic Oscillator","text":"Drift of the conserved energy H(t_textend) - H(0) — a proxy for the achievable accuracy at each precision:\n\nplot_energy_drift(df)","title":"Energy drift"},{"category":"section","location":"harmonic_oscillator/#Accuracy","page":"Harmonic Oscillator","text":"Maximum error against the analytic solution:\n\nplot_accuracy(df)","title":"Accuracy"},{"category":"section","location":"harmonic_oscillator/#Discussion","page":"Harmonic Oscillator","text":"Because the problem is linear, the implicit midpoint equations are linear too, so Newton converges in exactly one iteration per step for every line search, as does DogLeg. The choice of line search is therefore essentially irrelevant to the iteration count here.\nEvery configuration converges at Float16 and above: 72 of the 96 runs — eight solver configurations × three initial guesses at each of Float16, Float32 and Float64. That includes the Quadratic and BierlaireQuadratic line searches, which need SimpleSolvers 0.10 or later to converge here.\nBFloat16 converges only with NoInitialGuess (8/24), and the reason has nothing to do with the solver. With 8 significand bits the spacing of BFloat16 at t = 100 is 0.5, five times the step, so the time series 0:0.1:100 holds 1001 points but only 451 distinct values; the two extrapolating initial guesses are then handed two identical times and abort. At the coarse step the grid resolves and all 24 BFloat16 runs converge — the one place in this study where a larger time step helps.\nBisection stops at the requested tolerance; the others overshoot it. Its residual sits right at f_abstol = 8 eps(T), whereas the remaining line searches drive it a further two to three orders of magnitude down. All of them converge — the difference is how far past the tolerance they go, not whether they reach it. The results table flags this in the at_tolerance column.\nPicard (a fixed-point iteration) converges but needs many more iterations and is by far the slowest solver. Its iteration count is set by how far the tolerance is from the guess, so it rises with precision here: ≈ 1 per step at Float16, 2–3 at Float32, 7–9 at Float64.\nPrecision sets the achievable accuracy: the energy is conserved to roughly machine precision (≈ 7e-17 at Float64, 1e-7 at Float32, 4e-4 at both 16-bit formats), whereas the error against the analytic solution is set by the mathcalO(Delta t^2) midpoint discretization. That discretization error (1.47e-2) is reached exactly by Float32 and Float64; Float16 (2.0e-2 … 2.6e-2) and BFloat16 (3.0e-2 … 7.5e-2) sit above it, round-off rather than discretization limited — and the gap between the two 16-bit formats is the three significand bits between them.\nThe initial guess does not affect Newton (one exact step regardless), but it does affect Picard: MidpointExtrapolation gives the fewest iterations and NoInitialGuess (previous step) the most.","title":"Discussion"},{"category":"section","location":"harmonic_oscillator/#Results-table","page":"Harmonic Oscillator","text":"markdown_table(summary_table(df))","title":"Results table"},{"category":"section","location":"harmonic_oscillator/#harmonic_oscillator_dt1","page":"Harmonic Oscillator","text":"The same benchmark with a ten times larger step. For the linear oscillator the implicit equations stay linear, so Newton still converges in one iteration; the main effect is on Picard (many more iterations) and on the energy drift.\n\nspec1 = harmonic_oscillator_spec(timespan = (0.0, 100.0), timestep = 1.0)\ndf1   = cached_sweep(\"harmonic_oscillator_dt1.0\") do\n    run_benchmark(spec1; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Coarse time step (Δt = 1.0)"},{"category":"section","location":"harmonic_oscillator/#Convergence-2","page":"Harmonic Oscillator","text":"plot_convergence(df1; title = \"Harmonic Oscillator (Δt = 1.0)\")","title":"Convergence"},{"category":"section","location":"harmonic_oscillator/#Nonlinear-iterations-2","page":"Harmonic Oscillator","text":"plot_iterations(df1)","title":"Nonlinear iterations"},{"category":"section","location":"harmonic_oscillator/#Run-time-2","page":"Harmonic Oscillator","text":"plot_runtime(df1)","title":"Run time"},{"category":"section","location":"harmonic_oscillator/#Energy-drift-2","page":"Harmonic Oscillator","text":"plot_energy_drift(df1)","title":"Energy drift"},{"category":"section","location":"harmonic_oscillator/#Accuracy-2","page":"Harmonic Oscillator","text":"plot_accuracy(df1)","title":"Accuracy"},{"category":"section","location":"harmonic_oscillator/#Results-table-2","page":"Harmonic Oscillator","text":"markdown_table(summary_table(df1))","title":"Results table"},{"category":"section","location":"lotka_volterra_4d/#Lotka–Volterra-(4d)","page":"Lotka–Volterra (4d)","text":"The 4d Lotka–Volterra system is a larger non-canonical Hamiltonian system, benchmarked in its implicit form via iodeproblem. Its Hamiltonian H = a cdot q + b cdot log q (positions only) is used for the energy-drift metric. It is more strongly degenerate than the Lotka–Volterra (2d) system and is the most demanding test for the nonlinear solvers here. As for the 2d case the native time span (0 10) with Delta t = 001 is used, and then repeated for a ten times coarser step Delta t = 01 (see Coarse time step (Δt = 0.1)).\n\nThe benchmark below is regenerated at documentation build time with a single, fast timing pass. See the driver script scripts/midpoint_lotka_volterra_4d.jl for accurate BenchmarkTools measurements.\n\nusing SolverBenchmark\n\nspec = lotka_volterra_4d_spec()\ndf   = cached_sweep(\"lotka_volterra_4d_dt0.01\") do\n    run_benchmark(spec; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Lotka–Volterra (4d)"},{"category":"section","location":"lotka_volterra_4d/#Convergence","page":"Lotka–Volterra (4d)","text":"plot_convergence(df; title = \"Lotka–Volterra (4d)\")","title":"Convergence"},{"category":"section","location":"lotka_volterra_4d/#Nonlinear-iterations","page":"Lotka–Volterra (4d)","text":"Mean number of nonlinear-solver iterations per time step (converged runs only):\n\nplot_iterations(df)","title":"Nonlinear iterations"},{"category":"section","location":"lotka_volterra_4d/#Run-time","page":"Lotka–Volterra (4d)","text":"plot_runtime(df)","title":"Run time"},{"category":"section","location":"lotka_volterra_4d/#Energy-drift","page":"Lotka–Volterra (4d)","text":"Drift of the conserved Hamiltonian:\n\nplot_energy_drift(df)","title":"Energy drift"},{"category":"section","location":"lotka_volterra_4d/#Discussion","page":"Lotka–Volterra (4d)","text":"As for the 2d system, Newton (robust line search) and DogLeg converge in about 2 iterations per step at Float32/Float64 (slightly more than the 2d case, ≈ 2.3), while Picard never converges. As for the 2d system, Picard is the only failure at Float32/Float64.\nThe stronger degeneracy makes the 16-bit formats fail readily — the Newton linear solve raises a singular-Jacobian error for most configurations. Float16 reaches 14/24 here but only 6/24 at the coarse step; BFloat16 manages 2/24 and 6/24. Float32 and Float64 (21/24 each) again give essentially the same convergence pattern.\nBFloat16 never beats Float16 here either — the two tie at 6/24 at the coarse step, where the BFloat16 time grid is not itself a limitation. As for the 2d system, the obstacle is Jacobian conditioning, which wants significand bits rather than exponent range.\nThis example demonstrates the harness's robustness: configurations that throw a SingularException (rather than merely diverging) are caught and recorded as non-converged instead of aborting the sweep.","title":"Discussion"},{"category":"section","location":"lotka_volterra_4d/#Results-table","page":"Lotka–Volterra (4d)","text":"markdown_table(summary_table(df))","title":"Results table"},{"category":"section","location":"lotka_volterra_4d/#lotka_volterra_4d_dt01","page":"Lotka–Volterra (4d)","text":"The same benchmark over (0 10) with a ten times larger step. As for the 2d system the converging solvers need more iterations (≈ 3.4 per step instead of ≈ 2), the same configurations keep failing, and Float16 fails almost entirely.\n\nspec1 = lotka_volterra_4d_spec(timespan = (0.0, 10.0), timestep = 0.1)\ndf1   = cached_sweep(\"lotka_volterra_4d_dt0.1\") do\n    run_benchmark(spec1; timing = :quick, verbose = false, quiet = true)\nend\n\nnothing # hide","title":"Coarse time step (Δt = 0.1)"},{"category":"section","location":"lotka_volterra_4d/#Convergence-2","page":"Lotka–Volterra (4d)","text":"plot_convergence(df1; title = \"Lotka–Volterra (4d, Δt = 0.1)\")","title":"Convergence"},{"category":"section","location":"lotka_volterra_4d/#Nonlinear-iterations-2","page":"Lotka–Volterra (4d)","text":"plot_iterations(df1)","title":"Nonlinear iterations"},{"category":"section","location":"lotka_volterra_4d/#Run-time-2","page":"Lotka–Volterra (4d)","text":"plot_runtime(df1)","title":"Run time"},{"category":"section","location":"lotka_volterra_4d/#Energy-drift-2","page":"Lotka–Volterra (4d)","text":"plot_energy_drift(df1)","title":"Energy drift"},{"category":"section","location":"lotka_volterra_4d/#Results-table-2","page":"Lotka–Volterra (4d)","text":"markdown_table(summary_table(df1))","title":"Results table"},{"category":"section","location":"#SolverBenchmark","page":"Home","text":"SolverBenchmark benchmarks the nonlinear solver methods provided by SimpleSolvers.jl as they are used inside the implicit integrators of GeometricIntegrators.jl, applied to example problems from GeometricProblems.jl.\n\nEach problem is integrated with the implicit midpoint method, over the time span and step size that suit it ((0 100) at Delta t = 01 for the oscillator, pendulum and Toda lattice; (0 10) at Delta t = 001 for the stiffer Lotka–Volterra and double-pendulum systems), and each is also run at a ten times coarser step. The following options are swept:\n\nDimension Values\nPrecision BFloat16, Float16, Float32, Float64\nSolver Newton, DogLeg, Picard\nLine search (Newton only) Static, Backtracking, Bisection, Quadratic, BierlaireQuadratic, StrongWolfe\nInitial guess HermiteExtrapolation, MidpointExtrapolation, NoInitialGuess (previous step)\n\nThis yields eight solver configurations (six Newton line searches plus DogLeg and Picard) times four precisions times three initial guesses — 96 runs per problem.\n\nBoth 16-bit formats are swept because they divide the same 16 bits differently: Float16 keeps 11 significand bits and a 5-bit exponent, BFloat16 only 8 significand bits but an 8-bit exponent — the same dynamic range as Float32. Running both separates a failure caused by too few digits from one caused by overflow or underflow.\n\nFor every run the harness records whether the solver converged, the mean number of nonlinear iterations per time step, the run time, the residual, and — as an accuracy proxy — the drift of the conserved energy (and, where available, the error against the analytic solution).\n\nA second experiment set uses the neural-network variational integrator NonLinear_OneLayer_GML from NonlinearIntegrators.jl instead of implicit midpoint. Because that integrator's nonlinear system is near-singular, the sweep varies the solver's regularization factor lambda (in place of the initial guess): the lambda = 0 control plus six rungs of a ladder of multiples of sqrtvarepsilon(T), so that the shift is scaled to the precision it protects rather than fixed in absolute terms (see How the regularization factor scales). That runs across the four precisions and a reduced set of four solver configurations (Newton/Static, Newton/Backtracking, Newton/StrongWolfe, DogLeg), at the step sizes Delta t = 01 10 100 (ten steps each) — 112 runs per sweep.","title":"SolverBenchmark"},{"category":"section","location":"#Analyses","page":"Home","text":"","title":"Analyses"},{"category":"section","location":"#Implicit-Midpoint","page":"Home","text":"Harmonic Oscillator — a linear problem; the solvers converge in a single Newton iteration.\nPendulum — a nonlinear problem that exercises the solvers more.\nLotka–Volterra (2d) and Lotka–Volterra (4d) — non-canonical Hamiltonian systems built as iodeproblems (implicit ODE / degenerate Lagrangian form); stiffer problems where low precision starts to fail.\nDouble Pendulum — a chaotic hodeproblem (canonical Hamiltonian form) whose Hamiltonian depends on both q and p.\nToda Lattice — a 16-site hodeproblem, the highest-dimensional problem of the implicit-midpoint set.","title":"Implicit Midpoint"},{"category":"section","location":"#Nonlinear-Integrator","page":"Home","text":"Harmonic Oscillator (Nonlinear Integrator) — the one-layer network variational integrator, where a nonzero regularization factor is essential for the Newton solve to converge.\nPendulum (Nonlinear Integrator) — a nonlinear problem (built from the pendulum's phase-space iodeproblem).\nDouble Pendulum (Nonlinear Integrator) — a chaotic, strongly nonlinear system; DogLeg is the most robust solver here.\nToda Lattice (Nonlinear Integrator) — a 16-dimensional lattice, the highest-dimensional problem in the study.\n\nThe Lotka–Volterra systems are omitted from the nonlinear-integrator study: their degenerate Lagrangians are not currently supported by NonLinear_OneLayer_GML.","title":"Nonlinear Integrator"},{"category":"section","location":"#Key-findings","page":"Home","text":"The Key Findings page collects what the two experiment sets measured. In brief:\n\nNewton with a robust line search, and DogLeg, are the most efficient, at about 1.15 iterations per step on the oscillator and pendulum. All six line searches are comparably robust; the choice between them matters far less than the choice of solver, and Picard is the outlier that fails outright on four of the six problems.\nPrecision sets the achievable accuracy, and Float32 already reaches the mathcalO(Delta t^2) discretization error.\nThe two 16-bit formats show that the low-precision failures are about significand bits, not exponent range: BFloat16 never beats Float16 where the comparison is clean, despite its Float32-sized exponent. It has a separate limitation of its own — it cannot resolve a fine time grid.\nBisection stops at the tolerance it was asked for while the others overshoot it, so residuals must be read against f_abstol rather than against each other.","title":"Key findings"},{"category":"section","location":"#Reproducing-the-results","page":"Home","text":"The pages above regenerate their figures at documentation build time using a single, fast timing pass (timing = :quick). For accurate run-time measurements, run the driver scripts, which use BenchmarkTools and also write the raw results to results/:\n\njulia --project=. scripts/midpoint_harmonic_oscillator.jl\njulia --project=. scripts/midpoint_pendulum.jl\n\nscripts/run_all.jl runs every benchmark on this site — both experiment sets, at both step sizes — in one go.\n\nSee the API for the building blocks used to define new problems and assemble custom benchmarks.","title":"Reproducing the results"}]
}
